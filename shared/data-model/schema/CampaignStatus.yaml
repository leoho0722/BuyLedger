types:
  - name: CampaignStatus
    kind: enum
    doc: 開團是否仍在收單
    traits: [case-iterable, serializable, identity]
    cases:
      - name: ongoing
        doc: 仍在收單
      - name: closed
        doc: 已停止收單
