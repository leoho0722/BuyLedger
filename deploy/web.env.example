# web build-arg 環境變數範本 (NEXT_PUBLIC_*，build 期 inline)。複製為 deploy/web.env 後填入實值 (或 `make env`)。
# 由 Makefile 以 --env-file deploy/web.env 載入。

# 前端打後端的 base URL (例：http://localhost:4000/api)。
NEXT_PUBLIC_API_BASE_URL=

# Firebase web 設定。apiKey 與 appId 在 Firebase Console 註冊 Web app 後取得；
# authDomain/projectId/storageBucket/senderId 為專案層級值 (專案 buyledger-3330d)。
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=
