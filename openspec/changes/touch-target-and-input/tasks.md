## 1. 篩選膠囊元件化

- [x] 1.1 抽取前逐處記錄 OrdersView 與 OrdersCompactView 六處篩選膠囊現有的內距、字級與選取態差異，作為元件參數設計與驗收比對的基準。行為：抽取後的視覺差異可被逐項對照而非憑印象判斷。驗證：記錄結果寫入本 change 目錄下的比對筆記，六處各列出其現有數值。
- [x] 1.2 依決策〈六處篩選膠囊抽成共用元件而非逐處修補〉建立共用篩選膠囊元件，參數涵蓋標題、選取狀態與點擊動作，不提供六處用法以外的彈性，並依決策〈命中區加在標籤內部並以形狀宣告涵蓋整個膠囊〉使命中區達 44pt 且以膠囊形狀宣告。滿足 Filter chips meet the minimum hit region through a shared component。行為：六處膠囊視覺與變更前一致，相鄰膠囊各自可獨立點中。驗證：實機以 Accessibility Inspector 量測六處命中區皆不小於 44×44pt，並於相鄰膠囊邊界處點擊確認命中正確目標；以 1.1 的基準逐處比對視覺結果。
- [ ] 1.3 將 CampaignDetailView 收款狀態切換鈕的命中區補到 44pt，視覺尺寸不變。滿足 Data-mutating controls meet the minimum hit region。行為：該切換鈕可靠地被點中。驗證：實機以 Accessibility Inspector 量測命中區不小於 44×44pt。

## 2. 手勢的可見替代

- [x] 2.1 [P] 依決策〈手勢的可見替代放在既有的操作選單內，不新增獨立控制項〉，在 CampaignDetailView 的操作選單補上刪除項，沿用既有的刪除確認流程，並保留列表的長按刪除路徑。滿足 Gesture-only functions gain a visible alternative。行為：不使用長按即可刪除開團，且既有長按路徑仍可用。驗證：實機以語音控制或切換控制完成一次刪除開團；確認長按路徑行為未變。
- [x] 2.2 [P] 讓 OrderEditView 的商品明細提供可見的刪除入口，與既有的可見新增入口對稱，並保留既有的左滑刪除。滿足 Gesture-only functions gain a visible alternative。行為：不使用左滑即可刪除商品明細，且既有左滑路徑仍可用。驗證：實機以切換控制完成一次刪除商品明細；確認左滑路徑行為未變。

## 3. 輸入體驗

- [x] 3.1 [P] 為可自動填入的欄位宣告內容型別，並為自由文字欄位明確宣告不可填入以免系統誤判。滿足 Fillable fields declare their content type。行為：客戶名稱在裝置有聯絡人資料時出現填入建議，主檔與開團名稱不出現地址等無關建議。驗證：實機分別聚焦客戶名稱與主檔名稱欄位確認建議行為符合預期。
- [x] 3.2 [P] 依決策〈鍵盤工具列採系統標準工具列而非自製配件視圖〉，為 OrderEditView、FxView、QuoteView 與 SettingsView 中使用數字鍵盤的表單加上僅含完成動作的鍵盤工具列，並讓表單支援捲動收鍵盤；確認工具列不出現在具備 return 鍵的一般鍵盤上。滿足 Numeric keyboards provide a dismissal path。行為：數字鍵盤可由完成按鈕或捲動收起，收起後數值保留。驗證：實機於四處表單各驗一次完成按鈕與捲動收鍵盤；並聚焦一般文字欄位確認無多餘工具列。
- [x] 3.3 [P] 將 OrderEditView 表單內數值欄位的命中高度補到 44pt，使點在該列上下緣即可聚焦該欄位。滿足 Form value fields meet the minimum hit region。行為：三類數值欄位皆可由整列高度範圍內點擊聚焦。驗證：實機於單價、數值與百分比三類欄位各點擊列的上下緣確認聚焦。
- [x] 3.4 依決策〈焦點狀態放在功能狀態而非視圖本地狀態〉，在 OrderEditFeature 狀態加入依視覺順序定義的焦點欄位並由視圖綁定，開啟空白訂單時聚焦第一個欄位，表單關閉時清除焦點。滿足 Long forms manage focus order。行為：新訂單自動聚焦第一個欄位、欄位可依視覺順序跳轉、重開表單不沿用上次焦點。驗證：以 TestStore 撰寫測試斷言開啟空白訂單時焦點為第一個欄位、關閉時焦點清除；實機以外接鍵盤確認跳轉順序符合視覺順序。若自動聚焦導致鍵盤遮蔽表單上半部，依 Risks 所述改為不自動聚焦僅保留跳轉能力並記錄該取捨。

## 4. 照片檢視器手勢

- [ ] 4.1 依決策〈照片縮放以縮放倍率決定換頁是否接手〉，讓 BLPhotoViewer 支援縮放與雙點放大，雙點在一倍與兩倍間切換，倍率為一時換頁接手、大於一時改為平移且換頁停用。滿足 Photo viewer follows the standard gesture vocabulary。行為：照片可放大檢視商品標籤與金額細節，放大後可平移且不誤觸換頁，回到一倍後換頁恢復。驗證：實機分別於倍率為一與大於一兩種狀態測試左右滑動，確認各自為換頁與平移；雙點兩次確認回到原始尺寸。

## 5. 收尾與驗收

- [x] 5.1 將本次新增的刪除入口與鍵盤完成按鈕等字串補進 Localizable.xcstrings 的中英值，採文字插入方式而非全量重新序列化。行為：英文模式不露出中文 fallback。驗證：LocalizationCatalogTests 通過，並人工確認新增字串確實已收錄。
- [x] 5.2 重錄受膠囊元件化影響的 snapshot baseline，重錄前以 1.1 的基準逐張確認差異僅來自命中區調整而非視覺變化。行為：baseline 與新實作一致且視覺未偏移。驗證：snapshot 測試全綠，差異檢視結論記錄於 commit 說明。
- [x] 5.3 依文件同步鐵則檢視本次 diff：將「命中區加在標籤內部並以元件自身形狀宣告」與「焦點狀態下放 Feature.State」寫入 apps/ios/CLAUDE.md 對應章節。行為：後續實作者不需重新推導這兩條規則。驗證：對照根目錄 CLAUDE.md 的文件同步表逐列確認，結論為「有影響，已同步」或「確認無文件影響」。
