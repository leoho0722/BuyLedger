## 1. 行事曆依賴與純函式 helper

- [x] [P] 1.1 在 CalendarReminderClient.swift 新增 `CalendarReminderClient: Sendable` (比照 PhotoClient)：closures `requestAccess`、`addReminder(title:startDate:)->String`、`removeReminder(eventIdentifier:)`、`reminderExists(eventIdentifier:)`；liveValue 用 `EKEventStore` 建立當天全天事件 (`isAllDay`、`startDate == endDate`，附 `EKAlarm(relativeOffset: 9 * 3600)` 行程當天上午 9 點提示) 並存到預設行事曆，`removeReminder` 對不存在 identifier no-op；testValue 為可控 fake (授權成功、回傳固定 identifier、以記憶體集合模擬存在性)。行為：liveValue 能新增/移除系統行事曆事件。驗證：檔案編譯通過，且 CampaignFeatureTests 能以 testValue 覆寫跑通 add/remove flow〔設計決策：以 EventKit full access 建立與移除行事曆事件、CalendarReminderClient 依賴介面 (比照 PhotoClient)〕
- [x] 1.2 新增本機 SwiftData 連結儲存：`CampaignReminderRecord.swift` (@Model：`campaignID`、`eventIdentifier`，比照 `CampaignRecord` 不用 `@Attribute(.unique)`)、`CampaignReminderPersistence.swift` (@ModelActor：`fetchAll()->[String:String]`、`upsert(campaignID:eventIdentifier:)`、`delete(campaignID:)`)、`CampaignReminderRepository.swift` (`fetchLinks`/`saveLink`/`removeLink`，皆 async throws，type-based `DependencyKey`、共用 `PersistenceContainer.shared`；testValue 以自訂 `@unchecked Sendable` 記憶體容器實作)。行為：campaignID→eventIdentifier 連結寫入 SwiftData、讀回、清除且跨啟動保存。驗證：CalendarReminderTests 以 in-memory container 對 repository 做 save→fetch→remove round-trip 斷言〔設計決策：campaign→eventIdentifier 連結存於本機 SwiftData 新表；涵蓋需求 Campaign-to-event link is stored in local SwiftData〕
- [x] 1.2b 在 BuyLedgerSchema.swift 新增 `BuyLedgerSchemaV13` (models = V12 清單 + `CampaignReminderRecord`)、於 `BuyLedgerMigrationPlan.schemas` append V13、於 `stages` append `.lightweight(fromVersion: V12, toVersion: V13)`，並把 `PersistenceContainer.make` 的 `Schema(versionedSchema:)` 指向 V13。因只加新表、現有 top-level model 形狀不變，毋須凍結 V12 shadow。行為：既有 store 可 forward-only lightweight 遷到 V13、新表可用。驗證：`test_sim` 跑 SchemaMigrationTests (V10→V11、V11 reopen) 全綠、無 `Duplicate version checksums`〔設計決策：campaign→eventIdentifier 連結存於本機 SwiftData 新表〕
- [x] [P] 1.3 在 Campaign.swift 手寫 extension 新增 `func reminderDate(calendar: Calendar) -> Date` (回傳 `closeDate ?? openDate` 當天起始，用注入 calendar 的 `startOfDay(for:)`，不呼叫 Calendar.current) 與 `var reminderTitle: String` (回傳「「\(name)」訂購提醒」)。行為：提醒日期與標題規則為純函式、可脫離 EventKit 驗證。驗證：CalendarReminderTests 以固定 gregorian/UTC calendar 斷言 close date 當天起始、無 closeDate 退回 openDate、標題格式〔設計決策：提醒標題規則為純函式 helper；涵蓋需求 Order-reminder as an all-day event at a user-chosen date and time〕
- [x] [P] 1.4 在 Info.plist 新增 `NSCalendarsFullAccessUsageDescription`，說明用途為「將開團訂購提醒加入行事曆」。行為：App 首次請求行事曆完整存取時顯示此說明。驗證：build 通過，且 plist 內含該 key 與非空字串

## 2. CampaignEditFeature 意圖旗標

- [x] 2.1 在 CampaignEditFeature.State 新增 `var wantsReminder: Bool`，並於 init 加入對應參數 (預設 false)，交由 `BindingReducer` 驅動。行為：新增/編輯表單持有「是否要提醒」意圖、切換不觸發任何副作用。驗證：CampaignFeatureTests 新增 case 以 TestStore 對 `\.wantsReminder` 送 binding 並斷言 state 變更、無 effect〔涵蓋需求 Add/edit form defers reminder writes until save〕

## 3. CampaignFeature 接線

- [x] 3.1 在 CampaignFeature.State 新增 `var reminderEventIdentifiers: [Campaign.ID: String]`、`@Presents var reminderAccessAlert: AlertState<Action.ReminderAlert>?`，注入 `@Dependency(CalendarReminderClient.self)`、`@Dependency(CampaignReminderRepository.self)`、`@Dependency(\.calendar)`，並在 `.task` 以 `fetchLinks` 載入現有連結 (`reminderLinksLoaded`)。行為：進開團功能時還原每個開團的提醒狀態。驗證：CampaignFeatureTests 以 fake repository 預置連結，斷言 `.task` 後 `reminderEventIdentifiers` 被填入
- [x] 3.2 在 CampaignFeature 新增 detail 頁 action `addReminderTapped(Campaign.ID)`、`removeReminderTapped(Campaign.ID)`、`reminderStored(Campaign.ID, String?)`、`reminderAccessDenied`：add 先 `requestAccess`，成功則用 `campaign.reminderDate(calendar:)`/`reminderTitle` 呼叫 `addReminder`、以 `saveLink` 寫入 repository 與 state；被拒送 `reminderAccessDenied` 彈 alert；remove 呼叫 `removeReminder` 並以 `removeLink` 清 repository 與 state。行為：詳情頁即時新增/移除真實事件、被拒有提示。驗證：CampaignFeatureTests 以 fake client 覆蓋 add(成功/被拒)、remove 三路徑並斷言 state 與 alert〔涵蓋需求 Detail view creates and removes reminders immediately、Calendar access is requested lazily and denial is surfaced〕
- [x] 3.3 在 `.editCampaign(.presented(.saveTapped))` upsert 開團後 reconcile 提醒：依 `editState.wantsReminder` 與現有 eventIdentifier 差異決定建立/移除/先移除再重建 (名稱或 `reminderDate` 有變時)，更新 repository 與 `reminderEventIdentifiers`；並於 `newCampaignTapped`/`editCampaignTapped` 建立 edit state 時帶入 `wantsReminder` 初值 (既有開團=目前是否有提醒)。行為：表單儲存時把行事曆對齊意圖、取消不動行事曆。驗證：CampaignFeatureTests 覆蓋新建、移除、改名重建三種 save reconcile 情境〔設計決策：CampaignEditView 用意圖旗標、儲存時 reconcile；涵蓋需求 Add/edit form defers reminder writes until save〕
- [x] 3.4 在 reducer body 掛上 `.ifLet(\.$reminderAccessAlert, action: \.reminderAccessAlert)` 並定義 `ReminderAlert` case 與 PresentationAction。行為：權限被拒 alert 可呈現與關閉。驗證：CampaignFeatureTests 斷言 `reminderAccessDenied` 後 `reminderAccessAlert` 非 nil、dismiss 後歸零〔涵蓋需求 Calendar access is requested lazily and denial is surfaced〕

## 4. 兩頁 UI Row

- [x] [P] 4.1 在 CampaignEditView「開團資訊 Section」新增提醒 Row：依 `store.wantsReminder` 顯示藍色「新增提醒」或紅色 destructive「移除提醒」按鈕，點擊切換 `store.wantsReminder` (binding，不碰行事曆)。行為：編輯表單可切換提醒意圖且顏色/文案正確。驗證：實機 build and run 進新增開團頁，切換按鈕觀察藍↔紅、文案切換〔涵蓋需求 Reminder row appearance reflects reminder presence〕
- [x] [P] 4.2 在 CampaignDetailView `infoSection` 新增提醒 Row：依 `store.campaigns.reminderEventIdentifiers[campaign.id] != nil` 顯示藍色「新增提醒」或紅色 destructive「移除提醒」，分別送 `addReminderTapped`/`removeReminderTapped`；掛上 `reminderAccessAlert` 的 `.alert` 修飾子。行為：詳情頁即時新增/移除提醒並在被拒時彈 alert。驗證：實機 build and run 進開團詳情頁，新增後按鈕轉紅且系統行事曆出現事件、移除後轉藍且事件消失〔設計決策：CampaignDetailView 立即建立/移除；涵蓋需求 Reminder row appearance reflects reminder presence〕

## 5. 測試與驗收

- [x] 5.1 新增 CalendarReminderTests.swift：涵蓋 `Campaign.reminderDate(calendar:)`/`reminderTitle` (close date 當天起始、無 closeDate 退回 openDate、標題格式) 與 `CampaignReminderRepository` save/fetch/remove round-trip (in-memory container)，日期測試固定 `Calendar(identifier: .gregorian)` + `TimeZone(secondsFromGMT: 0)`。行為：提醒規則與連結儲存有回歸保護。驗證：`test_sim` 跑該檔全 pass
- [x] 5.2 在 CampaignFeatureTests.swift 補上第 2、3 節列出的 TestStore 案例 (wantsReminder binding、link 載入、detail add/remove/denied、save reconcile 三情境、alert 呈現/關閉)，以 `withDependencies` 覆寫 `CalendarReminderClient`/`CampaignReminderRepository`/`\.date`/`\.calendar` 的 fake。行為：行事曆接線 action flow 全數被 TestStore 鎖定。驗證：`test_sim` 跑 CampaignFeatureTests 全 pass
- [x] 5.3 build 前先 `cd apps/ios && agvtool next-version -all`，再對 iPhone 15 Plus 實機 `build_run_sim`/device build-and-run，於兩頁各操作一次並開系統行事曆 App 確認事件建立/移除。行為：端到端在實機可用。驗證：實機操作截圖或錄影呈現給使用者確認

## 6. 每團可設定的提醒時間

- [x] 6.1 `CampaignReminderRecord` 加 `minuteOfDay: Int`(當天第幾分鐘、預設 540＝09:00)；schema 升 V14：把 V13 的 `CampaignReminderRecord` 凍結為 shadow(保 V13 指紋)、新增 `BuyLedgerSchemaV14`(引用新 top-level)、append `.lightweight(V13→V14)`、`PersistenceContainer.make` 指向 V14。行為：既有 V13 store 可 lightweight 遷到 V14、新欄位帶預設。驗證：`test_sim` 跑 SchemaMigrationTests 全綠、無 `Duplicate version checksums`〔設計決策：每團可設定的提醒時間 (minuteOfDay，schema V14)〕
- [x] 6.2 `CampaignReminderPersistence`/`CampaignReminderRepository` 改用 `CampaignReminderLink`(`eventIdentifier` + `minuteOfDay`)：`fetchLinks()->[String:CampaignReminderLink]`、`saveLink(campaignID:eventIdentifier:minuteOfDay:)`。行為：連結與提示時間同筆讀寫。驗證：CalendarReminderTests round-trip 斷言含 minuteOfDay
- [x] 6.3 `CalendarReminderClient.addReminder` 改 `(title:date:alarmOffset:)`：建立全天事件並附 `EKAlarm(relativeOffset: alarmOffset)`。行為：提示時間由 caller 以每團 minuteOfDay 換算秒數傳入。驗證：檔案編譯通過、TestStore 以 fake 覆寫可斷言傳入的 offset
- [x] 6.4 `CampaignFeature.State` 的 `reminderEventIdentifiers` 換成 `reminderLinks: [Campaign.ID: CampaignReminderLink]`；`CampaignEditFeature.State` 加 `reminderTime`(Date，時分)並於 edit state 建立時帶入(新團 09:00、既有團取現值)；save reconcile 的重建條件加入提示時間變更，建立/重建以所選時間換算 alarmOffset、`saveLink` 帶 minuteOfDay。行為：表單設定的每團提示時間會反映到行事曆事件。驗證：CampaignFeatureTests 覆蓋自訂時間建立、僅改時間重建、預設時間三情境〔涵蓋需求 Per-campaign configurable reminder time〕
- [x] 6.5 UI：CampaignEditView 在意圖開啟時顯示「提示時間」`DatePicker(.hourAndMinute)`；CampaignDetailView 在有提醒時唯讀顯示「提示時間」列。行為：使用者可在編輯頁設定每團提示時間、詳情頁看得到目前設定。驗證：實機 build and run 觀察 picker 與唯讀列、行事曆事件提示時間符合所設
- [x] 6.6 build 前 bump build number，device build-and-run 到 iPhone 15 Plus，設一個非 09:00 的時間並開系統行事曆確認事件提示時間正確。行為：端到端在實機可用。驗證：實機呈現給使用者確認

## 7. 提醒改存時間戳、popup 選日期＋時間、詳情頁純顯示

- [x] 7.1 `CampaignReminderRecord` 的 `minuteOfDay` 改為 `reminderTimestamp: Date`；schema 升 V15：凍結 V14 的 `CampaignReminderRecord` shadow(保 V14 指紋)、新增 `BuyLedgerSchemaV15`、append `.lightweight(V14→V15)`、`PersistenceContainer.make` 指向 V15。`CampaignReminderLink`／persistence／repository 改帶 `reminderTimestamp`。行為：連結改存使用者自選時間戳、既有 V14 store 可 lightweight 遷到 V15。驗證：`test_sim` 跑 SchemaMigrationTests 與 CalendarReminderTests round-trip(含時間戳) 全綠〔設計決策：提醒改存使用者自選時間戳、popup 選日期＋時間、詳情頁純顯示 (schema V15)；涵蓋需求 Campaign-to-event link with timestamp is stored in local SwiftData〕
- [x] 7.2 全天事件的日期與提示改由時間戳導出：事件日期＝`calendar.startOfDay(for: reminderTimestamp)`、alarmOffset＝該時間戳當天分鐘數×60；`Campaign.reminderDate` 移除(日期不再掛結單日)。行為：行事曆事件為所選日期的全天事件、提示在所選時間。驗證：CampaignFeatureTests 斷言建立時傳入的 event date 與 offset 對應所選時間戳〔涵蓋需求 Order-reminder as an all-day event at a user-chosen date and time〕
- [x] 7.3 訂購提醒 popover 走 TCA、狀態在 reducer：`CampaignEditFeature.State` 加 `reminderTimestamp`(取代 `reminderTime`)、`draftReminderTimestamp`、`isReminderPickerPresented`；action `reminderPickerRequested`/`reminderPickerConfirmed`/`reminderPickerCancelled`/`removeReminderTapped` 各自處理草稿重設、提交、關閉、清意圖。CampaignEditView 點「新增提醒」／提醒時間列送 `reminderPickerRequested`，以 `.sheet(isPresented: $store.isReminderPickerPresented)` + `presentationDetents([.fraction(0.7)])` (螢幕 70% 高) 呈現 graphical `DatePicker([.date,.hourAndMinute])`(綁 `$store.draftReminderTimestamp`)、「加入提醒／完成」送 `reminderPickerConfirmed`、「取消」送 `reminderPickerCancelled`——view 不做 imperative 的 store 多步變更。行為：使用者以 sheet 自選日期＋時間、狀態流程全在 reducer。驗證：CampaignFeatureTests 覆蓋 requested/confirmed/cancelled/remove 四 action；實機操作 popover〔涵蓋需求 Reminder is configured via a date-and-time popup on the add/edit form〕
- [x] 7.4 CampaignDetailView 改純顯示：移除新增/移除按鈕與 picker，有提醒時才顯示 `LabeledContent("訂購提醒", value: CampaignFormatters.reminderTimestamp(...))`；移除 `addReminderTapped`/`removeReminderTapped` action 與 handler。行為：詳情頁不再即時管理提醒、僅顯示。驗證：實機 build and run 觀察詳情頁純顯示〔涵蓋需求 Detail view displays the reminder read-only〕
- [x] 7.5 save reconcile 的重建條件改為「名稱或 `reminderTimestamp` 變更」；`saveLink` 帶 `reminderTimestamp`；權限被拒仍走 `reminderAccessDenied` alert。行為：儲存時把行事曆對齊意圖與所選時間戳。驗證：CampaignFeatureTests 覆蓋自選時間戳建立、改名重建、改時間戳重建、清意圖移除、權限被拒 alert〔涵蓋需求 Add/edit form defers reminder writes until save、Calendar access is requested lazily and denial is surfaced〕
- [x] 7.6 build 前 bump build number，device build-and-run 到 iPhone 15 Plus：編輯頁 popup 選一個日期＋時間、儲存後開系統行事曆確認全天事件在該日、提示在該時間；詳情頁純顯示該時間戳。行為：端到端在實機可用。驗證：實機呈現給使用者確認
