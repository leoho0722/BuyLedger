<!--
TDD 模式：每個行為性任務先補上會 fail 的測試，再實作讓測試通過。tests / reducer 兩個檔案彼此獨立時可標 `[P]`，但實作與測試本身有先後關係，apply 階段須依序執行。
-->

## 1. 補測：定義跨頁深連結的期望行為

- [x] 1.1 在 `apps/apple/BuyLedgerTests/RootFeatureTests.swift` 新增測試 `categorySelectedJumpsToOrdersAndAppliesCategoryFilter`，斷言派發 `RootFeature.Action.categorySelected("beauty")` 後 `state.selectedTab == .orders`、`state.orders.selectedCategory == "beauty"`、`state.orders.searchText == ""`、`state.orders.selectedStatus == .all`、`state.orders.selectedDatePeriod == .all`、且 `state.orders.selectedOrderID` 為 filtered 後第一筆。對應 spec requirement「Deep link from analytics applies the category filter」。驗證：執行該測試在尚未動 reducer 前 fail，動完 reducer 後 pass（`xcodebuildmcp --log-level error test --filter RootFeatureTests/categorySelectedJumpsToOrdersAndAppliesCategoryFilter`）。
- [x] 1.2 在同檔案新增測試 `categorySelectedFiltersOrdersByExactCategoryFieldNotSearchText`，用 sampleOrders 模擬含「同名搜尋字串但 category 不同」的訂單，斷言過濾後僅包含 category 欄位等於 tapped 名稱的訂單，並不含 customer / item 名稱僅文字相符的訂單。對應 spec requirement「Deep link from analytics applies the category filter」中的精準欄位比對保證。驗證：執行該測試在尚未動 reducer 前因 `searchText` 路徑而 fail，動完 reducer 後 pass。
- [x] 1.3 修改既有測試 `smartGroupSelectedResetsDatePeriodAndPreviousStatus`，預先把 `state.orders.selectedCategory` 設為 `"beauty"`，於斷言內加上 `$0.orders.selectedCategory = nil`，覆蓋 spec requirement「Cross-feature navigations reset the category filter」中的 smart group 場景。驗證：執行 `RootFeatureTests/smartGroupSelectedResetsDatePeriodAndPreviousStatus` 在尚未動 reducer 前 fail，動完 reducer 後 pass。
- [x] 1.4 在同檔案新增測試 `customerSelectedResetsResidualCategoryFilter`，預先把 `state.orders.selectedCategory` 設為 `"snacks"` 並派發 `RootFeature.Action.customerSelected("Alice")`，斷言 `state.selectedTab == .orders`、`state.orders.searchText == "Alice"`、且 `state.orders.selectedCategory == nil`。覆蓋 spec requirement「Cross-feature navigations reset the category filter」中的客戶場景。驗證：執行該測試在尚未動 reducer 前 fail，動完 reducer 後 pass。

## 2. 修正 RootFeature 跨頁跳轉的篩選器狀態

- [x] 2.1 修改 `apps/apple/BuyLedger/Features/App/RootFeature.swift` 內 `Reduce { state, action in ... }` 的 `.categorySelected(category)` 分支，使其交付的行為為：`selectedTab = .orders`、`state.orders.selectedCategory = category`、`state.orders.searchText = ""`、`state.orders.selectedStatus = .all`、`state.orders.selectedDatePeriod = .all`、`state.orders.selectedOrderID = state.orders.filteredOrders(referenceDate: date.now).first?.id`。實作後 spec requirement「Deep link from analytics applies the category filter」對應的兩個 scenario（navigates and applies the filter / resets unrelated orders-list filters）皆能由 Task 1.1、1.2 的 TestStore 測試通過。驗證：執行 `xcodebuildmcp --log-level error test --filter RootFeatureTests/categorySelectedJumpsToOrdersAndAppliesCategoryFilter` 與 `RootFeatureTests/categorySelectedFiltersOrdersByExactCategoryFieldNotSearchText` 皆 pass。
- [x] 2.2 在同檔案的 `.smartGroupSelected(status)` 分支補上 `state.orders.selectedCategory = nil`，交付的行為為：跨頁套用 smart group 狀態篩選時清除殘留類別篩選；同時於 `.customerSelected(name)` 分支補上 `state.orders.selectedCategory = nil`，交付的行為為：客戶名深連結時清除殘留類別篩選。共同對應 spec requirement「Cross-feature navigations reset the category filter」。驗證：執行 `RootFeatureTests/smartGroupSelectedResetsDatePeriodAndPreviousStatus`、`RootFeatureTests/customerSelectedResetsResidualCategoryFilter`、以及既有 `smartGroupSelectedJumpsToOrdersAndAppliesStatus`、`smartGroupSelectionFlipsCorrespondingStatusChipPredicate` 全數 pass。

## 3. 整合驗證

- [x] 3.1 [P] 跑完整 `BuyLedgerTests` test suite，確認沒有其他既有測試因為 `selectedCategory` 預設值被動意外破壞，且本 change 新增／修改的測試全數 pass。驗證：先以 `xcodebuildmcp simulator list-sims` 取得當下可用 iPhone 模擬器名稱（依本專案規則不可 hardcode），代入 `xcodebuildmcp --log-level error test --scheme BuyLedger --destination 'platform=iOS Simulator,name=<resolved iPhone>'` 並確認 exit code 為 0。
- [x] 3.2 [P] iOS / iPadOS / macOS 三平台序列 build，確認 reducer 改動沒有破壞編譯（三平台共用同一份 `XCBuildData/build.db`，必須序列化）。驗證：先以 `xcodebuildmcp simulator list-sims` 取得當下可用 iPhone 與 iPad 模擬器名稱（不可 hardcode），代入 `xcodebuildmcp --log-level error build --scheme BuyLedger --destination 'platform=iOS Simulator,name=<resolved iPhone>' && xcodebuildmcp --log-level error build --scheme BuyLedger --destination 'platform=iOS Simulator,name=<resolved iPad>' && xcodebuildmcp --log-level error macos build --scheme BuyLedger`，要求整串 chain exit code 為 0（不可並行，避免 `database is locked`）。
- [x] 3.3 在 iOS Simulator 跑 build-and-run 後手動驗證：(a) 從「分析」分頁點任一類別 row → 進入「訂單」頁，對應類別篩選膠囊高亮，列表只剩該類別訂單；(b) 預先在訂單頁設定類別為某類別後，從「客戶」頁點客戶名跳回訂單頁，類別膠囊回到「全部」、搜尋欄帶入客戶名。驗證：兩個情境的視覺結果均符合 spec scenario 描述（人工 PASS／FAIL 記錄於本任務勾選備註）。
