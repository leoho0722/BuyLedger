<!--
本 change 為純 spec 契約修正，無 runtime code；各 task 的驗證目標 (diff / grep / spectra CLI) 即等同測試。
Delta specs 於封存時由 spectra archive 套進主 spec，apply 階段不手改主 spec，只做保真驗證與守門。
-->

## 1. Per-spec delta fidelity review

- [x] 1.1 [P] ai-order-summary: the `AI summary entry point gated by setting` requirement strips "and macOS" from the toolbar-action platform list, and the `Disabled-state prompt alert with deep link to settings` requirement drops the macOS preferences-window clause and removes the "Navigate to settings (macOS)" scenario, with all iOS/iPadOS clauses and scenarios preserved verbatim. Verify: unified diff of the change's specs/ai-order-summary/spec.md against the current main spec shows only macOS-token line changes.
- [x] 1.2 [P] data-model-codegen: the `Generated Swift owns the data shape and handwritten extensions own behavior` requirement's "Apple platforms build and tests pass after the split" scenario changes "iOS, iPadOS, and macOS builds / all three builds" to "iOS and iPadOS builds / both builds", with the existing apps/apple output paths and every other clause unchanged (path rename is out of scope). Verify: unified diff shows only the two build-scenario lines changed.
- [x] 1.3 [P] option-picker: the `Platform-adaptive option picker presentation` requirement is rewritten to specify only the iOS/iPadOS system List and its "macOS renders the card layout" scenario is removed, preserving the iOS/iPadOS List scenario verbatim. Verify: unified diff shows only the card/macOS clause and macOS scenario removed.
- [x] 1.4 [P] lookup-management: `Platform-adaptive lookup management presentation` and `Reconciliation status is a managed lookup kind` drop their card-on-macOS clauses down to the iOS/iPadOS system List, `Lookup item management operations are preserved across platforms` removes the "macOS rename and delete use the context menu" scenario and re-scopes its body to iOS/iPadOS, and `Payment method editing via the editor sheet` drops the "macOS context menu" entry point, with all iOS/iPadOS operations and scenarios preserved verbatim. Verify: unified diff shows only macOS-token line changes; the "iOS swipe-to-delete is retained" and all reconciliation-status and payment-method scenarios remain present.
- [x] 1.5 [P] order-batch-status-update: the `Orders list provides a multi-select mode` requirement changes "iOS, iPadOS, macOS, and Web" to "iOS and iPadOS", and `Batch status persistence is atomic on Apple` changes "Apple platforms (iOS, iPadOS, macOS)" to "Apple platforms (iOS and iPadOS)", preserving all scenarios verbatim. Verify: unified diff shows only the two platform-list lines changed and confirms no standalone "Web" token remains.
- [x] 1.6 [P] order-category-filter: the `Category filter on the orders list` requirement strips "and macOS", and `Category filter is presented as a trigger button with a searchable picker sheet` replaces every "iPadOS and macOS" / "iPadOS or macOS" with "iPadOS" across its body, example table, and scenarios, with the iOS-Compact filter-sheet scenario and the trigger-label example table preserved verbatim. Verify: unified diff shows only iPadOS-scoping line changes with no altered example rows or dropped scenarios.
- [x] 1.7 [P] order-merge: the `Order merge entry points` requirement strips "and macOS" from the merge-entry-point platform list, preserving all four merge scenarios verbatim. Verify: unified diff shows only the single platform-list line changed.

## 2. Change-level consistency and validation

- [x] 2.1 The change's delta capabilities exactly match the proposal's affected-specs list of seven, with no missing or extra capability. Verify: the change's specs/ directory contains exactly ai-order-summary, data-model-codegen, lookup-management, option-picker, order-batch-status-update, order-category-filter, and order-merge; `spectra analyze purge-macos-web-spec-residue` reports no Critical findings.
- [x] 2.2 No macOS token and no standalone Web token remains in any MODIFIED requirement body across the change's seven delta specs (apps/apple paths in data-model-codegen are intentionally retained and excluded from this check). Verify: a grep for macOS and for standalone Web across the change's specs/ returns zero matches.
- [x] 2.3 The change is valid and archive-ready. Verify: `spectra validate purge-macos-web-spec-residue` exits 0.
