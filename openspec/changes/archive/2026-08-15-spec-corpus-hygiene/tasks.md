## 1. 追溯區塊的閱讀規則

- [x] 1.1 讓讀規格的人與 AI 不再把追溯區塊裡的路徑當成現況：於路徑契約補上閱讀規則，明訂它是工具生成的歷史紀錄、其列出的路徑未經驗證，判斷現況應以搜尋程式碼為準。對應 spec requirement「Committed path references track the actual layout」。驗證：規格中可搜尋到該規則，且既有的「陳舊路徑掃描為空」情境維持成立。
- [x] 1.2 讓同一條規則出現在 AI 實際會讀到的地方：於根目錄協作指引的規格相關段落補上同一句話。驗證：對 CLAUDE.md 搜尋該規則可命中，且措辭與規格一致（CLAUDE.md）。
- [x] 1.3 確認規格正文本身沒有需要修正的陳舊路徑：以排除追溯區塊的方式掃描所有規格正文，確認命中僅剩路徑契約自身作為規則主體與前後對照表的部分。驗證：掃描結果為 8 處且全部位於路徑契約，逐一人工確認皆為刻意，不做修改。

## 2. 目的段落

- [x] 2.1 為 27 份跨領域政策型規格補上目的段落，使讀者能判斷一件事該歸哪一份規格：每份兩到三句，第一句陳述涵蓋範圍，第二句以「不涵蓋」指名至少一份相鄰規格，全部以正體中文撰寫，且不得使用規範性語彙或引入該規格沒有的約束。對應 spec requirement「Purpose sections are written for policy specs and deliberately left blank for feature specs」。驗證：27 份各自的目的段落不再是工具樣板，且每份都指名至少一份相鄰規格（openspec/specs 底下對應目錄）。
- [x] 2.2 讓目的段落的語言一致：把既有兩份以英文手寫的目的段落改為正體中文。驗證：全庫已填寫的目的段落皆為正體中文，無中英混雜。
- [x] 2.3 明文記錄其餘 23 份功能型規格的目的段落接受長期留白，使它不再被後續稽核重複報成缺陷。對應同一條 spec requirement 的留白情境。驗證：規格中可搜尋到該決定，且說明理由為目錄名已界定範圍。
- [x] 2.4 確認直接編輯目的段落的作法不會被後續歸檔覆寫：以既有兩份手寫目的段落歷經多次歸檔仍存在為證據，並在本次完成後跑一次歸檔驗證。驗證：規格驗證退出碼為 0，且目的段落內容未被改動。

## 3. 模板與指引的歧義

- [x] 3.1 讓提交訊息的協作者標記不再產生多種形狀：模板改為不含角括號佔位符的具體範例，並補一行說明模型名不加角括號、電子郵件固定、歷史提交不回溯。驗證：對 CLAUDE.md 搜尋角括號佔位符零命中；模板本身可直接複製使用（CLAUDE.md）。
- [x] 3.2 讓資料庫版本遷移指引的舉例可被對照：把已隨版本收斂而移除的版本號舉例改為引用現存版本，或只保留變更型態不提版本號。驗證：對該指引搜尋已移除的版本號零命中（.claude/skills/swiftdata-schema-migration/SKILL.md）。
- [x] 3.3 讓忽略清單的中文註解符合專案標點規範：兩處全形括號改為半形，並依規範在中文與半形內容之間補空格。驗證：對 .gitignore 搜尋全形括號零命中（.gitignore）。

## 4. 孤兒狀態檔

- [x] 4.1 清除從未落地的孤兒變更狀態檔，避免下次實作流程掃到啟動標記而把一個已放棄的功能誤喚醒：刪除前先把其中記錄的任務描述另存為備忘，再刪除兩個狀態檔。驗證：兩個檔案不存在；parked 清單與進行中清單皆不含該名稱。

## 5. 驗收

- [x] 5.1 執行整體驗收：規格驗證退出碼為 0；目的段落仍為樣板的份數由 50 降為 23；三處歧義的搜尋各自零命中。驗證：四項檢查逐一記錄結果。
- [x] 5.2 執行文件同步審視：本次差異命中「新的硬規則」與「讓既有描述失效」兩列，確認根目錄協作指引已同步，且無技術棧或目錄結構變動需要反映到說明文件。驗證：審視結論記為「有影響，已同步」。

## 修正輪紀錄

- 已補齊 `AGENTS.md` 的 Codex 專屬 trailer 範例與追溯區塊閱讀規則，並將 Purpose 硬規則同步到 `CLAUDE.md` 與 `openspec/config.yaml`。
- 已修正目的段落的相鄰規格歸屬說明：數字鍵盤收合路徑歸 `keyboard-dismissal-paths`，載入失敗的重試控制歸 `honest-state-feedback`，動態字級排除元件內距與指示元件縮放。
- 已補記 requirement delta 預先寫入 live spec 的 archive 冪等實測、現存 V15 → V16 migration 範例、長期留存孤兒備忘的括號格式，以及 `.xcodebuildmcp/` 忽略條目的 change 歸屬。
