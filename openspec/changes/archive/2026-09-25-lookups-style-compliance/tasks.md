## 0. 開工前基準

- [x] 0.1 確認逐檔待修明細與現況相符、測試環境可用，並補上讀取測試結果的規則：在 `apps/ios/CLAUDE.md`「不退回原生 xcodebuild／xcrun／simctl」的例外清單補一條「`xcrun xcresulttool` 唯讀讀取 result bundle 摘要 (XcodeBuildMCP 沒有等價指令)」，讓 0.2 起的結果讀取有依據；`findings.md` 已載入 8 個檔案的 302 筆項目，逐檔打開原始碼抽驗每個檔案至少兩筆，確認違規現在仍然成立；17 筆預填結論逐筆核對；以 `xcodebuildmcp simulator list` 確認 design「測試環境」的兩個 UDID 仍存在 (不存在就停下回報)。驗證：`grep -n "xcresulttool" apps/ios/CLAUDE.md` 有一行；8 個檔案各有抽驗記錄寫在本 task 下方，不成立的項目在 `findings.md` 就地寫明原因
  - 抽驗 (每檔至少兩筆)：`LookupCatalog.swift` 的 `Data Properties` MARK 與 `names(for:)` 單行 switch case；`LookupKind.swift` 的 `Cases` MARK 與 `Equatable, Hashable` 遵循；`LookupManagementDestination.swift` 的 `Destination.rename` associated value 說明與 `AddNameOnlyFeature.body` 空 reducer；`LookupManagementFeature.swift` 的 `paymentMethodIsCardless` 使用 `Dictionary(uniqueKeysWithValues:)` 與 State 四個 `@Presents`；`LookupManagementView.swift` 的 toolbar `.send(.addButtonTapped)` 未包 `.view` 與三個獨立 alert scope；`LookupNameEditorSheet.swift` 的 `showsDiscardConfirmation` 命名與 body 內 alert actions/message；`LookupCatalogTests.swift` 的 `Tests (Order Source / Category / Reconciliation Status)` MARK 與 `@testable import` 空行；`LookupManagementFeatureTests.swift` 的 `SwiftUI` 涵蓋 `Foundation` import 與檔案超過 300 行 (1,215 行)。各項均在現況成立。
  - 另有三筆 `LookupCatalogTests` 舊 finding 已不成立：L117 的 D1、TS3 現況已有 doc/`- Throws` 與 Given/When/Then；L129 的合併測試現況先加入「匯款」及已存在的「銀行匯款」，並斷言只剩一筆及三個旗標皆為 true。已於 `findings.md` 就地改記「報告誤報」及現況依據；其餘 findings 待後續 tasks 處理。
  - 17 筆預填結論逐筆核對：8 筆 H1 日期格式依本專案 `YYYY/M/D` 規則，8 個檔頭四行結構與日期皆符合；5 筆 TC5 對應現況 5 個 `some Reducer<State, Action>`；1 筆 `.inMemory` 對應 `LookupCatalog` 的 `@Shared` 記憶體共享；2 筆測試命名依單段 lowerCamel 既有裁決；1 筆 `canSubmit` 依 design Non-Goals 保留。預填結論未改動。
  - 模擬器：`xcodebuildmcp simulator list` 成功，列出 iOS 26.5 `iPhone 17` (`DDAA3311-B464-4DD3-96B8-360B26AF1929`) 與 `iPad Air 11-inch (M4)` (`6B65ED1C-3C2E-42BA-B1E7-08F606F175C5`)，兩台均 Booted。
  - 規則與驗證：`apps/ios/CLAUDE.md` 例外清單新增限定唯讀的 `xcrun xcresulttool get test-results summary --path <bundle>`；`grep -n "xcresulttool" apps/ios/CLAUDE.md` → 第 32 行；`findings.md` 八節筆數合計 302；預填結論計數為 H1 8、已登記例外 6 (其中 TC5 5、`.inMemory` 1)、測試命名 2、Non-Goals 1，合計 17；另記三筆目前已成立項目為誤報。
- [x] 0.2 取得單元與 UI 測試基準：iPhone 先以 `xcodebuildmcp simulator-management set-appearance --simulator-id <iPhone UDID> --mode light` 鎖淺色外觀並確認成功，再以 `BuyLedger.xctestplan` 跑完整單元回歸；接著 iPhone 與 iPad 各跑一次 `BuyLedgerUITests` 主回歸 (`--scheme BuyLedgerUITests --extra-args -only-testing:BuyLedgerUITests --extra-args -skip-testing:BuyLedgerUITests/LaunchPerformanceTests`)，iPad 前先單獨跑 `KeyboardDismissTests` 確認軟體鍵盤可顯示。驗證：本 task 下方記錄三份主結果 (單元、iPhone UI、iPad UI) 與一份鍵盤前置檢查結果的頂層 `totalTestCount`、通過數、失敗清單與 result bundle 絕對路徑 (以 `xcrun xcresulttool get test-results summary --path <bundle>` 讀取，不採用裝置層 `passedTests` 展開數)；已知 snapshot 雜訊以方法層 `-only-testing` (帶 `()`) 單獨重跑判定 [after: 0.1]
  - 阻塞：淺色外觀設定命令已由 `xcodebuildmcp simulator-management` 執行，但 CoreSimulatorService 連線失效並回報 `Operation not permitted` (exit code 1)。依常駐規則停止；單元回歸、iPhone/iPad UI 回歸、iPad `KeyboardDismissTests` 均未執行，沒有 result bundle。完整錯誤輸出記於本批回報。
  - 改由 Claude 在沙箱外執行 (2026-09-24，Codex 沙箱連不到 CoreSimulatorService)。先 `xcodebuildmcp simulator-management set-appearance --simulator-id DDAA3311-B464-4DD3-96B8-360B26AF1929 --mode light`，輸出 `Appearance successfully set to light mode`
  - 單元 (iPhone，`--scheme BuyLedger`)：頂層 `totalTestCount` 752、通過 752、失敗 0、跳過 0；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T03-10-27-572Z_pid37226_14f46d2b.xcresult`
  - UI 主回歸 iPhone (`--scheme BuyLedgerUITests --extra-args -only-testing:BuyLedgerUITests --extra-args -skip-testing:BuyLedgerUITests/LaunchPerformanceTests`)：`totalTestCount` 64、通過 64、失敗 0；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T03-11-20-981Z_pid38644_4b71e516.xcresult`
  - 鍵盤前置檢查 iPad (`-only-testing:BuyLedgerUITests/KeyboardDismissTests`)：`totalTestCount` 2、通過 2，軟體鍵盤可顯示；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T03-44-51-019Z_pid66085_c5a68f7e.xcresult`
  - UI 主回歸 iPad：`totalTestCount` 64、通過 63、失敗 1 (`OrderDetailTests/testCashOnDeliveryCorrectionPersistsAfterRelaunch()`，`Failed to determine hittability of "lookupManagement.paymentMethodEditor.cashOnDeliveryToggle" Switch: Activation point invalid`)；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T03-46-34-066Z_pid67570_a5dad889.xcresult`
  - 該條在 iPad 單獨重跑 (`-only-testing:BuyLedgerUITests/OrderDetailTests/testCashOnDeliveryCorrectionPersistsAfterRelaunch`)：`totalTestCount` 1、通過 1；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T04-13-12-300Z_pid86522_55f9c857.xcresult`。依 `apps/ios/CLAUDE.md` 判定為 hittability 環境雜訊；基準失敗清單以此為準：單元空、iPhone UI 空、iPad UI 空 (雜訊已排除)
- [x] 0.3 改動前先錄主檔管理的 snapshot：新增 `BuyLedgerTests/LookupCatalog+TestIsolation.swift` (`LookupCatalog.withIsolatedStorage`，以 `defaultInMemoryStorage = InMemoryStorage()` 隔離) 與 `BuyLedgerTests/SnapshotTests+Lookups.swift`，寫出 design「改動前先錄主檔管理的 snapshot 與 UI 測試」表列的五條測試 (四張主檔管理頁以 `NavigationStack` 包住) (`lookupManagementCategoryBaseline`、`lookupManagementPaymentMethodBaseline`、`lookupManagementEmptyBaseline`、`lookupManagementLoadFailureBaseline`、`lookupNameEditorSheetRenameBaseline`)，每條以 `TestDependencies.withFixedNow` 包住，store 用 `EmptyReducer()`、目錄在隔離 storage 內寫入，淺色外觀下錄製，基準圖落在 `__Snapshots__/SnapshotTests+Lookups/`；第一次執行自動錄製並回報失敗屬正常，逐張確認畫面內容後再跑一次。驗證：第二次以方法層 `-only-testing` (帶 `()`) 各自單獨通過且 `totalTestCount` ≥ 1；本 task 不動任何 production code；每張採用 `.image` 或 `drawHierarchyInKeyWindow` 的理由與 bundle 路徑記在本 task 下方 [after: 0.2]
  - 收尾記錄 (reviewer 複驗通過)：淺色外觀設定成功後，四張管理頁以 `drawHierarchyInKeyWindow` 重錄：`totalTestCount` 4、失敗 4，均為 `No reference was found on disk. Automatically recorded snapshot`；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T04-59-31-760Z_pid24170_53299739.xcresult`。reviewer 檢查四張新圖，導覽標題、「+」、清單、徽章、footer、空狀態與紅字皆正確。五條第二次比對 `totalTestCount` 5、通過 5；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-01-02-339Z_pid26221_190bb975.xcresult`。identifier 已在首次錄製前加入，與原定 task 順序不同；reviewer 確認不影響 snapshot 外觀。檢查器：本批 9 個 Swift 檔 `PASS`，`LookupManagementView.swift` 與 `LookupNameEditorSheet.swift` 例外延後至 task 4.1／4.2。
- [x] 0.4 改動前先錄主檔管理的 UI 測試：依 design 同一節，在 `BLAccessibilityID.LookupManagement` 新增 `addButton`、`deleteButton(_:)`、`nameField`、`nameSubmitButton`、`loadFailureMessage` 並掛到 `LookupManagementView` 與 `LookupNameEditorSheet` 對應的按鈕、輸入欄與載入失敗文字 (只加 identifier，不改其他程式碼)；新增啟動參數 `-BLUITestLookupWriteFailure` (`BLUITestConfiguration.shouldFailLookupWrites`、`LaunchOptions.shouldFailLookupWrites`，開啟時四種主檔 repository 的新增、刪除、改名拋錯、讀取不受影響) 與其 `BLUITestConfigurationTests` 解析測試；新增 `BuyLedgerUITests/Screens/LookupManagementScreen.swift` 與 `BuyLedgerUITests/Tests/More/LookupManagementTests.swift` 五條測試，seed、名稱與斷言依 design UI 測試表 (新增「手作小物」、「服飾」改名「衣著」並檢查新訂單類別選擇器、刪除「生活雜貨」、寫入失敗時新增「失敗類別」、首次載入失敗)。驗證：iPhone 上單獨跑 `LookupManagementTests`，新增、改名、刪除、首次載入失敗四條綠，「新增失敗」紅且失敗原因是 alert 未出現或清單出現該名稱 (新測試在改動前先失敗，是預期證據)，失敗訊息與 bundle 路徑記在本 task 下方；0.3 的五條 snapshot 以方法層重跑仍全綠 (identifier 不影響外觀)；`BLUITestConfigurationTests` 通過 [after: 0.3]
  - 收尾記錄 (reviewer 複驗通過)：`BuyLedgerTests/BLUITestConfigurationTests`：`totalTestCount` 19、通過 19；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T04-45-37-067Z_pid12642_eba5528e.xcresult`。iPhone 上 `BuyLedgerUITests/LookupManagementTests`：`totalTestCount` 5、通過 4、失敗 1；`testAddWriteFailureShowsAlertAndLeavesListUnchanged()` 以「主檔寫入失敗後「操作失敗」alert 未出現」失敗，是改動前預期紅燈；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-01-24-289Z_pid26628_6f7e607e.xcresult`。reviewer 確認本批 9 個 Swift 檔 `PASS`；`LookupManagementView.swift` 與 `LookupNameEditorSheet.swift` 的既有違規依裁決延至 task 4.1／4.2。

## 1. 主檔種類與目錄

- [x] 1.1 依「LookupKind 與 LookupCatalog 的分區與命名」整理 `LookupKind`：本體只放 case、顯示字串移到 `Computed Properties` extension、兩個訂單改寫方法移到 `Internal Method`，遵循改為 `String, CaseIterable, Sendable`，`addAlertTitle`／`addAlertMessage`／`addFieldPlaceholder` 改名 `addFormTitle`／`addFormMessage`／`nameFieldPlaceholder` 並同步 `Features/Lookups/` 內的呼叫端，case 與屬性的 doc 改寫成說明用途；`LocalizationCatalogTests` 的 `displayProperties` 把 `addAlertTitle` 換成 `addFormTitle`。驗證：App 與測試 target 編譯通過；`LookupCatalogTests`、`LookupManagementFeatureTests`、`RootFeatureTests`、`LocalizationCatalogTests` 以類別層 `-only-testing` 跑全綠；`grep -rn "addAlertTitle\|addAlertMessage\|addFieldPlaceholder" apps/ios/BuyLedger/Features/Lookups` 無輸出；變異驗證：在 `LookupKind.swift` 的 `case .category`／`addFormTitle`，將回傳字串 `"新增商品類別"` 暫改為 `"未收錄的新增主檔標題"`；執行 `BuyLedgerTests/LocalizationCatalogTests`，預期 `totalTestCount` > 0 且失敗，再還原字串確認全綠，記錄兩次 bundle 路徑 [after: 0.4]
  - 收尾記錄 (reviewer 複驗通過)：`LookupCatalogTests`、`LookupManagementFeatureTests`、`RootFeatureTests`、`LocalizationCatalogTests` 類別回歸共 `totalTestCount` 83 (12＋23＋36＋12)，通過 83；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-38-16-592Z_pid54253_b5411cb7.xcresult`。殘留搜尋 `addAlertTitle|addAlertMessage|addFieldPlaceholder` 限 `Features/Lookups` 為 0 筆。變異：將 `LookupKind.swift` 的 `.category.addFormTitle` 暫改為 `"未收錄的新增主檔標題"`，`LocalizationCatalogTests` 12 條中 `catalogContainsCompleteTraditionalChineseAndEnglishValues()` 失敗 (`missingCodeLiterals.isEmpty`)；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-40-10-382Z_pid56928_6085affd.xcresult`。每次變異還原後 SHA-256 與改動前相同；全部還原後 `LookupCatalogTests`＋`LocalizationCatalogTests` 24／24 通過，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-42-55-624Z_pid59747_5264ced7.xcresult`。
- [x] 1.2 依「付款方式旗標改為單一查詢」與「LookupKind 與 LookupCatalog 的分區與命名」整理 `LookupCatalog`：`Data Properties` 改 `Properties`、doc 改白話，新增 `paymentMethodFlags(named:)` (同名取第一筆、找不到回無旗標) 與 `replaceItems(of:from:)` (只換掉指定種類清單)，`SharedKey` 擴充移到新檔 `SharedKey+LookupCatalog.swift`；`LookupCatalogTests` 依「測試拆檔與寫法」重寫，合併旗標測試已在第 0 步修好、只套用寫法規範不改內容，並補 lookup-management spec「Payment method classification lookup tolerates duplicate names」範例表三列的參數化測試與 `replaceItems` 測試。驗證：`LookupCatalogTests` 全綠；變異驗證三項：1) 將 `paymentMethodFlags(named:)` 內 `paymentMethods.first { $0.name == name }` 暫改為 `paymentMethods.last { $0.name == name }`，以 `BuyLedgerTests/LookupCatalogTests/paymentMethodFlagsUsesFirstMatchingEntry()` 執行並確認重複名稱案例失敗；2) 將 `replaceItems(of:from:)` 的 `case .category` 賦值 `categories = catalog.categories` 暫改為 `categories = categories`，以 `BuyLedgerTests/LookupCatalogTests/replaceItemsChangesOnlyRequestedKind()` 執行並確認 category 案例失敗；3) 將 `renamedPaymentMethods` 旗標合併暫改為只取改名項目自己的旗標，確認既有 `renamingPaymentMethodMergesFlagsWhenEitherSideIsTrue()` 失敗。每次確認 `totalTestCount` > 0，還原後重跑全綠，記錄各 bundle 路徑 [after: 1.1]
  - 收尾記錄 (reviewer 複驗通過)：四類回歸的 `LookupCatalogTests` 為 12 條，並包含於 task 1.1 的 `83/83` 結果。`paymentMethodFlags(named:)` 改用 `.last` 後 `paymentMethodFlagsUsesFirstMatchingEntry(entries:name:expectedFlags:)` 失敗；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-40-53-350Z_pid57790_93fadc63.xcresult`。原訂 `categories = categories` 變異被編譯器以 `assigning a property to itself` 拒絕，不作為紅燈；改成 `categories = catalog.orderSources` 後 `replaceItemsChangesOnlyRequestedKind(kind:)` 失敗；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-42-21-802Z_pid59215_ebcb1dca.xcresult`。合併旗標改成只取 `info` 自己的旗標後 `renamingPaymentMethodMergesFlagsWhenEitherSideIsTrue()` 失敗 (`renamed.isBankTransfer`)；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-41-39-879Z_pid58492_13ddd9fb.xcresult`。全部還原後 `LookupCatalogTests`＋`LocalizationCatalogTests` 24／24 通過，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-42-55-624Z_pid59747_5264ced7.xcresult`；每次變異還原後 SHA-256 與改動前相同。
- [x] 1.3 把 `PaymentMethodEditPlan` 從 `LookupManagementFeature` 的巢狀型別搬到頂層新檔 `Features/Lookups/PaymentMethodEditPlan.swift`，欄位與 `Equatable`、`Sendable` 遵循不變，同步既有引用 (`LookupManagementFeature`、`LookupManagementFeatureTests`、`RootFeatureTests` 的建構點)。驗證：編譯通過；`grep -rn "LookupManagementFeature.PaymentMethodEditPlan" apps/ios --include='*.swift'` 無輸出；三個受影響測試類別全綠 [after: 1.2]
  - 收尾記錄 (reviewer 複驗通過)：task 原文所稱三個「受影響測試類別」實際只有兩個測試類別直接建構 `PaymentMethodEditPlan`：`LookupManagementFeatureTests` 與 `RootFeatureTests`；第三處是 production `LookupManagementFeature`，不是測試類別。task 原文保留。兩個測試類別及 `LookupCatalogTests`、`LocalizationCatalogTests` 同 task 1.1／1.2 一起回歸，總計 83/83，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T05-38-16-592Z_pid54253_b5411cb7.xcresult`；`grep -rn "LookupManagementFeature.PaymentMethodEditPlan" apps/ios --include='*.swift'` 為 0 筆。

## 2. 子 Feature

- [x] 2.1 依「主檔管理的 Action 分組」新增三個表單子 Feature 各自一檔 (`LookupAddFormFeature`、`LookupRenameFormFeature`、`PaymentMethodEditFormFeature`)，從 `tca/Feature.swift` 樣板複製：`view(.saveButtonTapped)` 驗證後送 `delegate(.saved)` (新增去頭尾空白且非空、改名另要求與原名不同、付款方式只要求非空)，注入的 `hasClassification`／`originalName`／`flags` 用 `let` 且無宣告處預設值，`body` 為 `Reduce(core)`，只 `import ComposableArchitecture` 與 `Foundation`。此 task 只新增型別，舊的巢狀子 reducer 暫時保留、尚未被引用。驗證：App target 以 `xcodebuildmcp simulator build` 編譯通過；三檔各自不超過 300 行 [after: 1.3]
  - 實作記錄 (初次交付狀態，已由下方收尾記錄複驗)：新增 `LookupAddFormFeature.swift` (72 行)、`LookupRenameFormFeature.swift` (76 行)、`PaymentMethodEditFormFeature.swift` (86 行)。三者都從 TCA Feature 樣板建立，使用 `Reduce(core)`、`view`／`delegate` 分組與各自文件指定的名稱驗證；未修改舊的 `LookupManagementDestination.swift` 或父層。檢查器逐檔結果皆為 `TOTAL {} files 1 PASS`；初次交付時尚未執行 build，編譯結果見下方收尾記錄。
  - 驗證清單 (交 reviewer)：`build | BuyLedger | App target | iPhone 17 (DDAA3311-B464-4DD3-96B8-360B26AF1929) | 預期編譯通過，確認三個目前尚無呼叫端的 Feature 型別可加入 target`
  - 收尾記錄 (reviewer 複驗通過)：第三次執行 `BuyLedgerTests/PaymentMethodCorrectionFeatureTests` 時 `totalTestCount` 5、通過 5，App target 與測試 target 均編譯通過，三個表單 Feature 隨 App target 一併編譯；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T06-45-21-242Z_pid7316_370307c9.xcresult`。三個表單 Feature 檢查器結果皆為 `TOTAL {} files 1 PASS`。
- [x] 2.2 依「付款方式更正流程抽成子 Feature」新增 `PaymentMethodCorrectionFeature` 與 `BuyLedgerTests/PaymentMethodCorrectionFeatureTests.swift`：沒有自己的 View、不設 `view` 分組，`requested` 以 `LookupCatalog.paymentMethodFlags(named:)` 判斷旗標是否變更後取樣，零受影響或旗標未變直接原子寫入，否則存 `pendingPlan` 並送 `confirmationRequired(affectedOrderCount:)`，`confirmed`／`cancelled`／`editResponse`／`planResponse` 行為照 design；測試涵蓋取樣篩選與正規化、確認筆數、取消不寫入、寫入失敗不動主檔、各旗標變更與目錄缺項，替身用 `LockIsolated`、訂單用 `LedgerOrder.fixture(...)`、`receive` 用 case key path。此 task 只新增型別與測試，父層尚未接上。驗證：`PaymentMethodCorrectionFeatureTests` 全綠；兩檔各自不超過 300 行 [after: 2.1]
  - 實作記錄 (初次交付狀態，已由下方收尾記錄複驗)：新增 `PaymentMethodCorrectionFeature.swift` (178 行) 與 `PaymentMethodCorrectionFeatureTests.swift` (299 行)。Feature 照 design 以 `@Shared(.lookupCatalog)` 讀旗標、取樣與正規化受影響訂單、依確認條件原子寫入，沒有 `View` 或 `view` Action 分組；測試涵蓋取樣篩選與正規化、零筆直接寫入、旗標未變直接寫入、三種旗標變更確認、目錄缺項、確認與取消、取樣／原子寫入失敗及失敗時目錄不變。測試依賴以 `LockIsolated` 記錄寫入次數、使用隔離目錄與窮舉 `TestStore`。五個本批 Swift 檔的檢查器逐檔結果皆為 `TOTAL {} files 1 PASS`；初次交付時的編譯與測試修正見下方記錄。
  - 編譯修正記錄 (後續複驗前)：reviewer 首次執行 `BuyLedgerTests/PaymentMethodCorrectionFeatureTests` 時，在 `PaymentMethodCorrectionFeature.swift` 的取樣與寫入 Effect 各遇到一則 `any Error` 無法轉為對應 typed error 的編譯錯誤，未產生 result bundle。兩段已改成 `do throws(PersistenceError)` 與 `do throws(PaymentMethodPersistenceError)`；後續複驗結果見收尾記錄。
  - 測試編譯修正記錄 (後續複驗前)：reviewer 第二次執行 `PaymentMethodCorrectionFeatureTests` 時回報缺少 `Foundation`、private `DirectScenario` 被非 private 測試方法使用、`@Test(arguments:)` 讀取 MainActor 隔離的 `changedFlags`，以及取樣替身的 thrown error 型別不符，均未產生 result bundle。已補 `Foundation`、將 `DirectScenario` 移到非 private 的 `Nested Types` extension、將 `changedFlags` 改為 `nonisolated` 並在同一行註明 Swift Testing 會於 actor 隔離外讀取，以及讓取樣與寫入替身分別宣告 `throws(PersistenceError)`／`throws(PaymentMethodPersistenceError)` 並直接拋出具體錯誤值。`PaymentMethodCorrectionFeatureTests.swift` 檢查器輸出 `TOTAL {} files 1 PASS`；後續複驗結果見收尾記錄。
  - 收尾記錄 (reviewer 複驗通過)：第三次執行 `BuyLedgerTests/PaymentMethodCorrectionFeatureTests` 時 `totalTestCount` 5、通過 5；App target 與測試 target 均編譯通過；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T06-45-21-242Z_pid7316_370307c9.xcresult`。變異驗證 (各次還原後 SHA-256 一致)：`flagsChanged` 改為 `==` 後三條失敗 (`requestedDirectScenariosWriteWithoutConfirmation(_:)`、`requestedFiltersAndNormalizesOrdersBeforeConfirmation()`、`requestedWithChangedFlagsRequiresConfirmation(_:)`)，三條皆為 `Received unexpected action`；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T06-46-12-972Z_pid8177_e47b1971.xcresult`；移除 `.cancelled` 的 `pendingPlan = nil` 後 `confirmationChoiceWritesOrClearsPendingPlan(_:)` 失敗 (`A state change does not match expectation`)，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T06-46-47-632Z_pid8731_0444fa57.xcresult`；移除取樣名稱篩選後 `requestedFiltersAndNormalizesOrdersBeforeConfirmation()` 失敗，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T06-47-21-818Z_pid9261_b905c0b6.xcresult`。全部還原後 5／5 通過，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T06-47-55-292Z_pid9753_b57d5720.xcresult`。五個本批 Feature／測試檔的檢查器結果皆為 `TOTAL {} files 1 PASS`。
  - 驗證清單 (交 reviewer)：
    - `build | BuyLedger | App target | iPhone 17 (DDAA3311-B464-4DD3-96B8-360B26AF1929) | 預期編譯通過，確認新 Feature 與測試 target 可編譯`
    - `單元 | BuyLedger | BuyLedgerTests/PaymentMethodCorrectionFeatureTests | iPhone 17 (DDAA3311-B464-4DD3-96B8-360B26AF1929) | 預期五個測試方法全綠，參數化情境涵蓋三種旗標、三種直接寫入、確認／取消與兩種失敗`
    - `變異 | BuyLedger | BuyLedgerTests/PaymentMethodCorrectionFeatureTests/requestedWithChangedFlagsRequiresConfirmation() | iPhone 17 (DDAA3311-B464-4DD3-96B8-360B26AF1929) | 在 PaymentMethodCorrectionFeature.swift 將 let flagsChanged = currentFlags != flags 改為 let flagsChanged = currentFlags == flags，預期計畫的 flagsChanged 與確認流程斷言轉紅；還原後全綠`
    - `變異 | BuyLedger | BuyLedgerTests/PaymentMethodCorrectionFeatureTests/confirmationChoiceWritesOrClearsPendingPlan() | iPhone 17 (DDAA3311-B464-4DD3-96B8-360B26AF1929) | 在 .cancelled 分支移除 state.pendingPlan = nil，預期取消情境轉紅；還原後全綠`
    - `變異 | BuyLedger | BuyLedgerTests/PaymentMethodCorrectionFeatureTests/requestedFiltersAndNormalizesOrdersBeforeConfirmation() | iPhone 17 (DDAA3311-B464-4DD3-96B8-360B26AF1929) | 在 preparePlan 移除 .filter { order in order.paymentMethod == originalName }，預期未相關訂單混入計畫的斷言轉紅；還原後全綠`

## 3. 父層切換

- [x] 3.1 一次完成父層切換 (這幾項互相引用，無法分批編譯)：依「主檔管理的拆分：付款方式更正子 Feature、讀寫輔助型別與三個表單子 Feature」「主檔管理的 Action 分組」「寫入成功才更新主檔目錄」「寫入失敗改為一次性提示」「四個 @Presents 併成單一 Destination」「讀寫分派收進 LookupItemOperations」「reducer 不 import SwiftUI」改寫 `LookupManagementFeature`；以 `git mv` 把 `LookupManagementDestination.swift` 改名為 `LookupManagementFeature+Destination.swift` 並改寫成含 `@ReducerCaseIgnored` alert 與明寫 `Action` 的 `Destination`、刪除舊的四個巢狀子 reducer；新增 `LookupItemOperations.swift` 作為全庫唯一依主檔種類選資料來源的地方 (lookup-management spec「Adding a lookup kind is a compile-time obligation」)，改名在訂單表寫入失敗時以補償寫入把主檔表改回舊名 (使用者 2026-09-24 裁決，見 design「讀寫分派收進 LookupItemOperations」)；`LookupManagementView` 只做必要改動 (所有送出改 `.view(...)`、三個 sheet (`add`、`rename`、`editPaymentMethod`) 與一個 alert 改用列舉 scope、`hasLoadFailed` 與 `classification(for:)` 取代舊欄位)；新增 `LookupItemAddition.swift`、`LookupItemRename.swift` 兩個頂層型別；`RootFeature` 只把兩處主檔攔截改為 `delegate(.itemRenamed)` 與 `delegate(.paymentMethodEdited)`。行為必須符合 lookup-management spec「Lookup item management operations are preserved across platforms」：寫入成功才改目錄、失敗顯示一次性 alert。驗證：App target 以 `xcodebuildmcp simulator build` 編譯通過；`Features/Lookups/` 內非 View 檔的 `import SwiftUI` 為 0；`LookupManagementFeature.swift` 不含檔頭與空行不超過 300 行 (超過就停下回報，不壓行)；design 驗收條件 4 的殘留名稱在 production 檔中為 0。本 task 完成到 3.2 完成之間，測試 target 可能無法編譯，不跑單元測試 [after: 2.2]
  - 實作記錄 (2026-09-24，待 reviewer 驗收；3.1 保持未勾選)：一般 `mv` 將 `LookupManagementDestination.swift` 改名為 `LookupManagementFeature+Destination.swift`，新檔保留原建立日期 `2026/5/23`；新增 `LookupItemOperations.swift`、`LookupItemAddition.swift`、`LookupItemRename.swift` (新增檔日期 `2026/9/24`)。`LookupManagementFeature` 改為單一 `Destination` 與 `correction` 子 Feature，`Action` 按 design 分組；新增、刪除、載入、改名與付款方式更正都在成功回應後更新共享目錄。`LookupItemOperations` 持有五個 repository，Effect 使用 `do throws(PersistenceError)`；`Destination` 以 `@ReducerCaseIgnored` alert case、明寫 `Action`、遵循 `Equatable` 的巢狀 `Alert`／`WriteFailure` 與 `Destination.State` alert builder 實作。alert builder 回傳 `Destination.State`，以 `.alert(AlertState(...))` 包裝；呼叫端直接指定 `.deleteConfirmation(...)`、`.retroactiveConfirmation(...)` 或 `.writeFailure(...)`。兩個 reducer 檔只 import `ComposableArchitecture` 與 `Foundation`，alert 文案直接放在 `TextState(...)` 字面值。`LookupManagementView` 只調整新 Action、三個列舉 scope、表單分派與新 State 讀取；`RootFeature` 只改兩處為 delegate action。檢查器對本次修正的 `LookupManagementFeature.swift` 與 `LookupManagementFeature+Destination.swift` 均回報 `TOTAL {} files 2 PASS`；目前 `LookupManagementFeature.swift` 為 270 行 (排除檔頭與空行)。
  - 機械檢查：本批 7 個 Swift 檔執行 `python3 /private/tmp/claude-501/-Users-leoho-Develop-BuyLedger/d4e52ef9-0db8-485b-b7ba-a3127d2503b5/scratchpad/step4-checks/lookups_checks.py apps/ios/BuyLedger/Features/Lookups/LookupManagementFeature.swift apps/ios/BuyLedger/Features/Lookups/LookupManagementFeature+Destination.swift apps/ios/BuyLedger/Features/Lookups/LookupItemOperations.swift apps/ios/BuyLedger/Features/Lookups/LookupItemAddition.swift apps/ios/BuyLedger/Features/Lookups/LookupItemRename.swift apps/ios/BuyLedger/Features/Lookups/LookupManagementView.swift apps/ios/BuyLedger/Features/App/RootFeature.swift`；六個非 View 檔各為 `TOTAL {} files 1 PASS`。`LookupManagementView.swift` 報 8 筆既有項目 (MK1 ×3、MK5 ×1、DOCBL ×3、MOD ×1)，依先前裁決留待 task 4.1，這批沒有整理該檔其他分區或格式。
  - 先前殘留名稱核對：`rg -n 'import SwiftUI' apps/ios/BuyLedger/Features/Lookups -g '*.swift'` 僅列 `LookupManagementView.swift` 與 `LookupNameEditorSheet.swift`，非 View 檔 0 筆。當時限定 production 的命令 `rg -n 'paymentMethodIsCardless|paymentMethodIsBankTransfer|paymentMethodIsCashOnDelivery|uniqueKeysWithValues|renameRequested|paymentMethodEditSucceeded|LookupManagementDestination|deletionConfirmation|retroactiveConfirmation:|writeFailureAlert|addAlertTitle|addAlertMessage|addFieldPlaceholder' apps/ios/BuyLedger/Features/Lookups apps/ios/BuyLedger/Features/App/RootFeature.swift -g '*.swift'` 無輸出。依使用者裁決，design 驗收條件 4 的完整範圍為 `apps/ios/BuyLedger/Features/Lookups/`、`apps/ios/BuyLedger/Features/App/RootFeature.swift`，以及 `LookupManagementFeatureTests*.swift`、`PaymentMethodCorrectionFeatureTests.swift`、`LookupCatalogTests.swift`、`RootFeatureTests.swift`；本 task 的殘留掃描只涵蓋目前已切換的 production 子範圍，測試檔會在 task 3.2／3.3 更新後納入最終檢查。此段記錄的是補償寫入裁決前的狀態；第 6 批依使用者 2026-09-24 新裁決，已在 `LookupItemOperations.renameItem` 加入主檔表回復補償，見下方第 6 批記錄。
  - 本次編譯修正 (依 reviewer 提供的 App target 錯誤)：原錯誤指出 `.deleteConfirmation`、`.retroactiveConfirmation`、`.writeFailure` 被當成 `AlertState` 成員，且 `Destination.State` 因 `Alert` 不遵循 `Equatable` 而編譯失敗。已讓 `Destination.Alert` 遵循 `Equatable`；三個 `Destination.State` alert builder 改回傳 `Self` 並包裝 `.alert(AlertState(...))`；六個呼叫點改成直接指定 `.deleteConfirmation(...)`／`.retroactiveConfirmation(...)`／`.writeFailure(...)`，alert 文字與按鈕不變。尚待 reviewer 重跑 App target build。
  - 本次唯讀驗證：`python3 /private/tmp/claude-501/-Users-leoho-Develop-BuyLedger/d4e52ef9-0db8-485b-b7ba-a3127d2503b5/scratchpad/step4-checks/lookups_checks.py apps/ios/BuyLedger/Features/Lookups/LookupManagementFeature.swift apps/ios/BuyLedger/Features/Lookups/LookupManagementFeature+Destination.swift` → `TOTAL {} files 2 PASS`；`rg -n 'import SwiftUI' apps/ios/BuyLedger/Features/Lookups -g '*.swift'` 只列 `LookupManagementView.swift`、`LookupNameEditorSheet.swift`；殘留 pattern 限 `Features/Lookups/` 與 `RootFeature.swift` → 無輸出；`awk 'NR > 6 && NF { count++ } END { print count }' apps/ios/BuyLedger/Features/Lookups/LookupManagementFeature.swift` → `270`。完整殘留範圍另含 `LookupManagementFeatureTests*.swift`、`PaymentMethodCorrectionFeatureTests.swift`、`LookupCatalogTests.swift`、`RootFeatureTests.swift`，待 task 3.2／3.3 更新後檢查。
  - 驗證清單 (交 reviewer)：`build | BuyLedger | App target | iPhone 17 (DDAA3311-B464-4DD3-96B8-360B26AF1929) | 預期編譯通過`；重跑兩條 grep，第一條確認 Lookups 非 View 檔的 `import SwiftUI` 為 0，第二條先查目前 task 的 `Features/Lookups/` 與 `RootFeature.swift` production 子範圍，完整 acceptance 範圍待 task 3.2／3.3 完成後一併驗證；重跑上述 `awk` 命令，預期 270。依常駐規則本環境未執行 build、測試或模擬器操作。
  - 第 6 批修正記錄 (2026-09-24；3.1 保持未勾選)：依 reviewer 複驗修正 `State.id`、`items`、`hasClassification` 的大括號換行、Destination builder 的單參數簽章、`addButtonTapped` 的巢狀括號排版，並把單一呼叫點的付款方式完成 helper 內聯。依新裁決，`LookupItemOperations.renameItem` 先改主檔表、再改訂單表；訂單表失敗時呼叫主檔表反向改名，補償失敗以 `AppLogger.persistence.error` 記錄固定訊息，最後仍拋出原訂單表錯誤。`renameLookupItem` 與 `renameOrders` 各自以完整 `switch kind` 分派。此前 reviewer 在 AlertState 修正後提供 App target build 通過；該結果早於本批補償程式碼，本批版本仍待 reviewer 重跑 build。唯讀量測 `awk 'NR > 6 && NF { count++ } END { print count }' apps/ios/BuyLedger/Features/Lookups/LookupManagementFeature.swift` 為 `264`，取代本 task 前段 `270` 的舊量測值；`rg -n 'import SwiftUI' apps/ios/BuyLedger/Features/Lookups -g '*.swift'` 只列 `LookupManagementView.swift`、`LookupNameEditorSheet.swift`。
  - 收尾記錄 (reviewer 複驗通過)：App target 隨單元測試編譯通過。Claude 代修一處：`LookupItemOperations.swift` 補 `import OSLog` (MemberImportVisibility)。補償寫入的兩次變異驗證都讓 `LookupManagementFeatureTests/orderRenameFailureRestoresTheStoredLookupName()` 轉紅：(1) 補償呼叫的 `newName` 改為 `rename.newName`，`totalTestCount` 1、失敗 1，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T11-10-31-428Z_pid4412_3c9884c4.xcresult`；(2) 整段補償 `do/catch` 移除，`totalTestCount` 1、失敗 1，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T11-11-27-406Z_pid5356_deae247f.xcresult`；兩次還原後 SHA-256 `6d106cc6cade7cacacca9b6ce73e19f7825742c14eac5c2344093ea907b66721` 與改動前一致。checker 33 檔 PASS，只有依裁決延後到 4.1／4.2 的兩個 View 檔未過。
  - 後續：本 task 的補償寫入已由 9.1 的單一交易取代 (使用者 2026-09-25 裁決)。
- [x] 3.2 依「測試拆檔與寫法」改寫 `LookupManagementFeatureTests`：拆成主檔與 `LookupManagementFeatureTests+Forms.swift` (超過 300 行時依同規則再拆)，刪除已搬到 `PaymentMethodCorrectionFeatureTests` 的更正流程測試，改用 `LookupCatalog.withIsolatedStorage`；補寫入成功才更新目錄、寫入失敗 alert 與目錄不變、改名的訂單表寫入失敗時主檔表收到「新名→舊名」的補償改名且不送 `itemRenamed` (spec 範例「category rename with a failing order rewrite」)、補償也失敗時仍回報改名失敗 (lookup-management spec 新增的失敗範例表各列，含付款方式更正的取樣失敗與原子寫入失敗各一條：子 Feature 送 `delegate(.failed)` 後 `destination` 為付款方式編輯失敗 alert，關閉後回 `nil`)、失敗 alert 關閉後不再出現、表單驗證、改名成功送出 `itemRenamed`、付款方式表單轉送 `correction(.requested)` 與完整確認流程；四種主檔的載入與新增以 `LookupKind.allCases` 參數化。同時把 `SnapshotTests+Lookups.swift` 的載入失敗 State 由 `errorMessage` 改為 `hasLoadFailed = true` (唯一允許的測試碼改動，不重錄基準圖)。驗證：`LookupManagementFeatureTests` 相關類別與 `PaymentMethodCorrectionFeatureTests` 全綠；每個測試檔不超過 300 行 [after: 3.1]
  - 第 6 批實作記錄 (2026-09-24；3.2 保持未勾選)：`LookupManagementFeatureTests.swift` 拆為主檔與 `+Failures.swift`、`+Forms.swift`、`+PaymentMethodCorrection.swift`、`+Rename.swift`、`+Support.swift`，各檔均低於 300 行；載入／新增以 `LookupKind.allCases` 參數化，失敗測試採 spec 例表的「手工藝品」、「服飾」及「信用卡」貨到付款旗標資料，涵蓋操作失敗、付款方式取樣與原子寫入失敗、補償成功與補償失敗。另只將 `SnapshotTests+Lookups.swift` 的載入失敗 state 改為 `hasLoadFailed = true`。本批測試尚未由 Codex 執行；待 reviewer 跑 `LookupManagementFeatureTests` 全部拆檔、`PaymentMethodCorrectionFeatureTests` 與五條主檔 snapshot 方法層 selector。機械檢查命令 `python3 /private/tmp/claude-501/-Users-leoho-Develop-BuyLedger/d4e52ef9-0db8-485b-b7ba-a3127d2503b5/scratchpad/step4-checks/lookups_checks.py $(git status --short --untracked-files=all -- '*.swift' | awk '$1 != "D" { print $2 }' | rg -v 'LookupManagementView\.swift$|LookupNameEditorSheet\.swift$')` 輸出 `TOTAL {} files 33 PASS`；依既有裁決排除兩個 View 檔。`git diff --check` 無輸出。完整殘留 pattern 搜尋無命中；`import SwiftUI` 搜尋只列兩個 View 檔。六個 `LookupManagementFeatureTests*.swift` 實體行數依序為 257、248、187、222、135、97，皆未超過 300 行。
  - 收尾記錄 (reviewer 複驗通過)：`LookupManagementFeatureTests` (含五個拆分檔) 16 條、`PaymentMethodCorrectionFeatureTests` 5 條、`TestSuiteIntegrityTests` 3 條、五條 Lookups snapshot 全綠且未重錄；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T11-09-40-762Z_pid3531_7851cba0.xcresult` (`totalTestCount` 66、失敗 0)。共享目錄斷言依 TCA `SharingState.md` Tests 節寫在第一個觀察到變更的斷言點。
  - 後續：本 task 的補償測試與 spec 範例「category rename with a failing order rewrite」已由 9.1 的單一交易測試與新範例取代。
- [x] 3.3 依「測試拆檔與寫法」改寫 `RootFeatureTests` 直接送出主檔內部 action 的 10 條 (逐條名稱見 design，含 `renamingReconciliationStatus...InOneReducerCall`)，讓它們經由子層 `view` 與 `delegate` 走完整流程，驗證 lookup-management spec「Lookup data has a single source shared by every consumer」修改後的行為：改名寫入成功後目錄更新，`RootFeature` 收到 `itemRenamed` 才改寫記憶體訂單，寫入失敗時不送 delegate、訂單不變；四條 `renaming...InOneReducerCall` 依 design 改名為 `renaming...RewritesOrdersThroughDelegate` 並改斷言，新增 `renamingCategoryWriteFailureLeavesOrdersUnchanged` (改寫 10 條、新增 1 條)；其餘測試不動。驗證：`RootFeatureTests` 全綠；design 列的殘留名稱 `grep` 在 `RootFeatureTests.swift` 無輸出；`TestSuiteIntegrityTests` 通過 (`.off` 總數未增加) [after: 3.2]

  - 第 6 批實作記錄 (2026-09-24；3.3 保持未勾選)：依 design 改寫 10 條 RootFeature 流程測試、改名四條 rename 測試，新增 `renamingCategoryWriteFailureLeavesOrdersUnchanged`；rename 流程由 lookup 子 Feature 的 `.view`、表單 `.view`、成功 response 與 `.delegate(.itemRenamed)` 驅動，新增失敗案例驗證不改目錄或記憶體訂單。Root 測試中的 `setOrdersAndProjections` 使用具名 closure 參數。測試尚未由 Codex 執行，待 reviewer 跑 `RootFeatureTests` 與 `TestSuiteIntegrityTests`。
  - 收尾記錄 (reviewer 複驗通過)：`RootFeatureTests` 37 條全綠 (同一 bundle)；design「測試拆檔與寫法」的殘留 grep 已由 reviewer 收窄為只比對舊 state 欄位與舊 action 形狀 (`?\.retroactiveConfirmation`、`\.retroactiveConfirmation\(`)，以收窄後的命令執行無輸出。
  - 驗收文字衝突已裁決：design grep 收窄為舊形狀，新的 `Destination.State.retroactiveConfirmation(affectedOrderCount:)` 呼叫不列入
## 4. 畫面

- [x] 4.1 依「LookupManagementView 與 LookupNameEditorSheet」完成 `LookupManagementView` 的整理：分區為 Properties → Body → Private Views → Private Method → Preview，`body` 只放清單與整頁 modifier 並依四組排序，新增鍵抽成 `addButton`，清單列抽成 `Features/Lookups/Components/LookupItemRow.swift` (含兩個 `#Preview`)，整個清單再抽成 `Features/Lookups/Components/LookupItemList.swift` (使用者 2026-09-24 裁決，見 design 同一節)，改名送出只送一個 `.view(.saveButtonTapped(name:))`，`palette` 與改名表單標題放 Private Method，補四個具名 `#Preview`。驗證：0.3 的四條主檔管理 snapshot 以方法層單獨重跑全綠且未重錄；`LookupManagementView`、`LookupItemRow`、`LookupItemList` 各自不超過 300 行且 checker PASS [after: 3.3]
  - 已依使用者裁決抽出 LookupItemList
  - 收尾記錄 (reviewer 複驗通過)：曾有一次編譯失敗，Claude 已修正 `LookupItemList` 的初始化引數順序。修正後四條主檔管理 snapshot、`LocalizationCatalogTests`、`AccessibilityConventionScanTests`、`ActionGroupingScanTests` 共 24 條全綠，PNG 未重錄且 mtime 仍是 0.3 錄製時間；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-04-54-213Z_pid48151_9a9285d2.xcresult`。iPhone `LookupManagementTests` 刪除與改名 2 條全綠；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-06-43-149Z_pid50313_736c7e13.xcresult`。三檔 checker PASS。`LookupItemList.body` 修正後四張 snapshot 已於同一 bundle 重跑通過。
- [x] 4.2 依同一節整理 `LookupNameEditorSheet`：Properties 依 `@Environment` → `@State`／`@FocusState` → `let` 排列且同組字母序，`showsDiscardConfirmation` 改名 `isDiscardConfirmationPresented`，alert 的按鈕與訊息抽成 Private Views，doc 改白話。驗證：`lookupNameEditorSheetRenameBaseline` 以方法層單獨重跑通過且未重錄；`LookupManagementTests` 的新增與改名兩條 (會開名稱表單) 在 iPhone 單獨跑全綠 [after: 4.1]
  - 第 8 批實作記錄：整理 `LookupNameEditorSheet.swift`，保留自訂 `init` 參數順序，四個呼叫端不需調整；將 `LookupItemList.body` 改為直接組合 `List`。當時 checker 對 init closure 參數箭頭誤報 D3，依指示未添加錯誤的 `- Returns` 文件；reviewer 後續修正 checker 並完成複驗。
  - 收尾記錄 (reviewer 複驗通過)：`lookupNameEditorSheetRenameBaseline()` 與四條管理頁 snapshot、`LocalizationCatalogTests`、`AccessibilityConventionScanTests`、`ActionGroupingScanTests` 共 25 條全綠且未重錄基準圖；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-18-13-617Z_pid61497_3c6e41fa.xcresult`。iPhone `LookupManagementTests` 新增與改名 2 條全綠；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-19-33-731Z_pid63143_f48f3ecf.xcresult`。checker 的 D3 對 init closure 參數箭頭誤報已由 reviewer 修正為只檢查頂層參數列之後的回傳箭頭。

## 5. 守門

- [x] 5.1 依「Action 分組守門涵蓋主檔與子層 store」更新 `ActionGroupingScanTests`：`migratedViewPaths` 與 `migratedComponentDirectories` 納入 Lookups，`storeSendPattern` 改為同時比對 `store` 與以 `Store` 結尾的駝峰識別字，`ActionGroupingScanTests+Scenarios.swift` 補三列自我測試。驗證：`ActionGroupingScanTests` 全綠；兩次變異驗證 (在 `LookupManagementView` 暫加子層 store 送非 `.view` action、在 `LookupManagementFeature` 暫加 `.destination(.presented(.rename(.view(` 比對) 各自讓對應規則轉紅，xcresult 可解析且 `totalTestCount` > 0，還原後轉綠，四份 bundle 路徑記在本 task 下方 [after: 4.2]

  - 第 9 批實作記錄 (待 reviewer 驗收)：改動 `ActionGroupingScanTests.swift`、`ActionGroupingScanTests+Scenarios.swift`，並依新規則補齊 SWC 列出的 switch case 空行；清除 `LookupManagementFeature.swift` 尾隨空白。design 只列入 `LookupManagementView.swift`，其中集中子層 store 的送出 closure；`LookupNameEditorSheet.swift` 無 store 送出，未納入 `migratedViewPaths`。checker `TOTAL {} files 11 PASS`；檔案行數見本批回報。`git diff --check HEAD` 與未追蹤檔尾隨空白搜尋皆無輸出。未執行 build 或測試，兩組變異與 `ActionGroupingScanTests` 回歸待 reviewer 執行；task 保持未勾選。
  - 收尾記錄 (reviewer 複驗通過)：`ActionGroupingScanTests`、`LookupManagementFeatureTests`、`PaymentMethodCorrectionFeatureTests`、`RootFeatureTests`、`LookupCatalogTests`、`TestSuiteIntegrityTests`、`LocalizationCatalogTests`、`AccessibilityConventionScanTests` 與五條 Lookups snapshot 共 98 條全綠，基準圖未重錄；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-39-24-858Z_pid84992_a1f6f3f2.xcresult`。變異 1 (`LookupManagementView` 的 `renameStore` 多送 `.delegate(.saved(...))`)：`migratedViewsSendOnlyViewActions()` `totalTestCount` 1、失敗 1，違規訊息指向該 `.delegate` 送出，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-40-51-353Z_pid86804_21b7fe85.xcresult`；還原後 SHA-256 `bba5e253ecc6e81f28e7f472431525f44a57c248f45995f7b6b9918dc1ddc0ba` 與改動前一致，重跑 1／1 通過，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-42-33-971Z_pid88696_2cc23bad.xcresult`。變異 2 (`LookupManagementFeature` 加 `.destination(.presented(.rename(.view(.saveButtonTapped(name: _)))))` case)：`reducersDoNotSendOrInterceptChildViewActions()` `totalTestCount` 1、失敗 1，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-41-43-247Z_pid87837_cac0262e.xcresult`；還原後 SHA-256 `5eb7e6379ba48f55f7d8e4159cbe5f8e1d6472fcbced542cc1053914e6831c7b` 一致，重跑 1／1 通過，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T12-43-10-474Z_pid89255_86f65eda.xcresult`。checker 39 檔 PASS (含新增的 `SWC`)；`git diff --check HEAD` 與未追蹤檔尾隨空白檢查皆無輸出。

- [x] 5.2 依「表單關閉後才顯示後續 alert」在 `LookupManagementFeature` 加 `isFormSheetDismissing`、`pendingAlert` 與 `View.Action.formSheetDismissed`，四個 alert 來源改經同一入口，`LookupManagementView` 三個 `.sheet` 加 `onDismiss`；`LookupManagementFeatureTests` 補：表單關閉前到達的新增失敗先暫存、`formSheetDismissed` 後才顯示；表單關閉後到達的失敗直接顯示；付款方式更正的回溯確認同樣暫存；取消表單時 `formSheetDismissed` 不產生 alert；並調整既有失敗與更正測試的斷言。UI 測試新增 `LookupManagementTests/testDeleteWriteFailureShowsAlertAndLeavesListUnchanged` (lookup-management spec 失敗範例表的刪除列)。驗證：相關單元測試全綠；檔案不超過 300 行；兩次變異驗證 (`formSheetDismissed` 不移出 `pendingAlert`、`addResponse(.failure)` 略過暫存直接設 `destination`) 各自讓對應單元測試轉紅，還原後轉綠 [after: 5.1]

  - 第 10 批實作記錄 (2026-09-24；當時 5.2 保持未勾選)：`LookupManagementFeature` 新增關閉中提示暫存狀態與共用呈現入口，三個表單儲存分支標記關閉中，`formSheetDismissed` 將暫存提示移入 `destination`；`LookupManagementView` 的三個 sheet 以 `onDismiss` 送出該 action。新增 `LookupManagementFeatureTests+AlertTiming.swift` 覆蓋新增失敗先到、關閉完成先到、付款方式回溯確認延後與取消表單；既有新增、改名、付款方式與 `RootFeatureTests` 流程改為驗證關閉狀態與提示轉移。`LookupManagementTests` 新增刪除寫入失敗案例，重用既有 `LookupManagementScreen.deleteItem(named:)`。
  - 靜態驗證：當時 `lookups_checks.py` 輸出 `TOTAL {} files 10 PASS`；`LookupManagementFeature.swift` 為 298 行 (不含檔頭與空行)，低於 300 行；`git diff --check HEAD` 與未追蹤 Swift／Markdown／YAML／JSON 尾隨空白搜尋皆無輸出。當時未執行 build、測試、模擬器或變異，reviewer 結果記於下方。
  - 收尾記錄 (reviewer 複驗通過)：checker 40 檔 PASS；`git diff --check HEAD` 無輸出；未追蹤檔尾隨空白 grep 無輸出。`LookupManagementFeatureTests`、`PaymentMethodCorrectionFeatureTests`、`RootFeatureTests`、`ActionGroupingScanTests`、`LocalizationCatalogTests`、`TestSuiteIntegrityTests` 與五條 Lookups snapshot 共 87 條全綠，基準圖未重錄；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T13-26-10-715Z_pid31938_cf02c089.xcresult`。變異 1 (`formSheetDismissed` 不移出 `pendingAlert`) 2 條全失敗，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T13-27-45-378Z_pid33970_e53c0ba0.xcresult`；變異 2 (新增失敗略過暫存) 1 條失敗，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T13-28-31-367Z_pid34777_37eda4b9.xcresult`。兩次還原後 SHA-256 為 `a27b2396ecfb72395cdf738a36b2356386c4dfa28c01a9f2007179fa5a58aab9`；重跑 2／2 通過，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T13-29-20-093Z_pid35550_46ba6333.xcresult`。iPhone `testAddWriteFailureShowsAlertAndLeavesListUnchanged` 與 `OrderDetailTests` 全綠；`testDeleteWriteFailureShowsAlertAndLeavesListUnchanged` 當時失敗，修正記錄見下方。
  - 第 10 批 UI 修正記錄 (2026-09-24)：`LookupManagementScreen.requestDeletion(named:file:line:)` 負責滑動、開啟刪除確認並送出請求，不等待 alert 消失；`deleteItem(named:file:line:)` 呼叫它後等待 alert 關閉。刪除失敗 UI 測試改以 label 含「操作失敗」的 alert 查詢等待失敗提示，再關閉並檢查項目仍在清單。checker 命令 `python3 /private/tmp/claude-501/-Users-leoho-Develop-BuyLedger/d4e52ef9-0db8-485b-b7ba-a3127d2503b5/scratchpad/step4-checks/lookups_checks.py apps/ios/BuyLedgerUITests/Screens/LookupManagementScreen.swift apps/ios/BuyLedgerUITests/Tests/More/LookupManagementTests.swift` 輸出 `TOTAL {} files 2 PASS`；`git diff --check HEAD` 無輸出，未追蹤檔尾隨空白搜尋無輸出。未執行 build、測試或模擬器，待 reviewer 在 iPhone 與 iPad 複驗。

  - UI 收尾記錄 (reviewer 複驗通過)：iPhone `LookupManagementTests` 六條與 `OrderDetailTests` 共 10 條全綠；刪除失敗 alert 正確出現並關閉，確認 alert 接續失敗 alert 未重現時序問題；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T13-43-55-530Z_pid49853_8d8a9032.xcresult`.

- [x] 5.3 依「iPad 側邊欄切分頁先清「更多」路徑」在 `RootFeature` 新增 `sidebarTabSelected(RootTab)` (先清 `morePath` 再切分頁)、`smartGroupSelected` 開頭先清 `morePath`，`RootSidebarLayout` 側邊欄選取改送 `sidebarTabSelected`；`RootFeatureTests` 補三條：`sidebarTabSelected` 清空 `morePath` 並切分頁、`smartGroupSelected` 清空 `morePath`、`tabSelected` 保留 `morePath` (iPhone 行為不變)；`.claude/rules/ios-navigation.md` 的「從「更多」下的 pushed 頁深連結到其他根分頁」一條補上側邊欄切分頁與智慧分組同樣先清、iPhone `TabView` 的 `tabSelected` 保留堆疊。驗證：`RootFeatureTests` 全綠；變異驗證 (`sidebarTabSelected` 不清 `morePath`) 讓對應測試轉紅、還原後轉綠；iPad `LookupManagementTests/testRenameCategoryUpdatesLookupAndOrderPicker` 不再閃退 [after: 5.2]

  - 第 11 批實作記錄 (2026-09-24；5.3 保持未勾選)：`RootFeature.Action` 新增 `sidebarTabSelected(RootTab)`，reducer 先清空 `morePath` 再更新 `selectedTab`；`smartGroupSelected` 先清空 `morePath`；`RootSidebarLayout.selectionBinding` 的一般分頁改送新 action，`RootTabLayout` 保持不變。`RootFeatureTests` 新增 `sidebarTabSelectedClearsMorePathAndChangesTab()`、`smartGroupSelectedClearsMorePath()`、`tabSelectedKeepsMorePath()` 三條 GWT 測試。`.claude/rules/ios-navigation.md` 補上 iPad 側邊欄與智慧分組清路徑、iPhone `TabView` 保留堆疊的現行規則。

  - 靜態驗證：checker 對 `RootFeature.swift` 與 `RootFeatureTests.swift` 輸出 `TOTAL {} files 2 PASS`；`git diff --check HEAD` 無輸出；未追蹤檔尾隨空白搜尋無輸出。檢查器目前的 `B_GROUP` 未含 `RootSidebarLayout.swift`，三檔一起執行時會將它誤按 A 組整檔檢查並列出既有 findings (`LEN` 1、`MK1` 8、`MKN` 5、`D9` 2、`BRACE` 1、`SWC` 18、`DOCBL` 6)。依本批 B 組範圍人工核對該檔唯一 diff：`store.send(.tabSelected(tab))` 改為 `store.send(.sidebarTabSelected(tab))`。檔案實體行數：`RootFeature.swift` 507、`RootSidebarLayout.swift` 414、`RootFeatureTests.swift` 2092。未執行 build、測試或模擬器。

  - 變異建議：在 `RootFeature.swift` 的 `sidebarTabSelected` case 移除 `state.morePath.removeAll()`，預期 `RootFeatureTests/sidebarTabSelectedClearsMorePathAndChangesTab()` 轉紅；還原後預期通過。
  - 收尾記錄 (reviewer 複驗通過)：`RootFeatureTests`、`ActionGroupingScanTests`、`TestSuiteIntegrityTests` 共 48 條全綠，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T14-39-38-103Z_pid1775_04cb3a98.xcresult`。變異 (`sidebarTabSelected` 不清 `morePath`)：`sidebarTabSelectedClearsMorePathAndChangesTab()` `totalTestCount` 1、失敗 1，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T14-41-07-604Z_pid4126_2ae23a55.xcresult`；還原後 SHA-256 `7fe30aed797335692ce4531b51c789a79a935a5a33277ae7f38d92b0718455c8` 一致，重跑 1／1 通過，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T14-41-48-544Z_pid4843_58ad27ac.xcresult`。reviewer 另把 `smartGroupSelected` 的註解改為說明真正原因 (路徑非空時切分頁會讓 iPad 崩潰)，checker 的 B 組補上 `RootSidebarLayout.swift`；checker 41 檔 PASS。iPad 閃退已修正，見 6.1 記錄。

## 6. UI 驗收

- [x] 6.1 以 UI 測試實際驗證主檔操作 (lookup-management spec「Lookup item management operations are preserved across platforms」)：iPhone 與 iPad 各單獨跑 `LookupManagementTests` 六條與 `OrderDetailTests` (付款方式回溯重算流程)。驗證：兩台都全綠，其中「新增失敗」由 0.4 的紅轉綠；結果與 bundle 路徑記在本 task 下方 [after: 5.3]
  - 收尾記錄 (reviewer 複驗通過)：iPad (`6B65ED1C-3C2E-42BA-B1E7-08F606F175C5`) 鍵盤預檢 `KeyboardDismissTests` 2／2，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T14-42-35-138Z_pid5818_cdc354e7.xcresult`；iPad `LookupManagementTests` 六條與 `OrderDetailTests` 共 10 條全綠 (含先前閃退的改名流程)，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T14-44-39-287Z_pid7542_f40c4f40.xcresult`；iPhone (`DDAA3311-B464-4DD3-96B8-360B26AF1929`) 同組 10 條全綠 (「新增失敗」由 0.4 的紅轉綠、新增的「刪除失敗」通過)，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T14-50-30-748Z_pid12121_3e2e1569.xcresult`。過程中先後抓到兩個真缺陷並已修正：表單關閉時 alert 被丟掉 (5.2)、iPad 側邊欄切分頁閃退 (5.3)。

## 7. 文件同步

- [x] 7.1 依「主檔管理的拆分：付款方式更正子 Feature、讀寫輔助型別與三個表單子 Feature」把 `apps/ios/CLAUDE.md` 的「大型 reducer 以同域輔助型別拆分，不拆成子 reducer」改寫成 design 引用的條文，其下四條子項保留。驗證：`grep -n "不拆成子 reducer" apps/ios/CLAUDE.md` 無輸出，新條文提到 `QuoteRateFeature`、`PaymentMethodCorrectionFeature`、`OrdersFilterOperations`、`LookupItemOperations` [after: 6.1]
- [x] 7.2 同步其餘文件：`.claude/rules/ios-data-layer.md` 的主檔更名 cascade 改為「`RootFeature` 在收到 `itemRenamed` delegate 時改寫」、付款方式編輯資料流改為 `PaymentMethodCorrectionFeature` 取樣與確認 → `PaymentMethodPersistence` 落盤 → `RootFeature` 攔截 `paymentMethodEdited` delegate；`.claude/rules/ios-unit-tests.md` 的隔離 storage 參考改為 `LookupCatalog.withIsolatedStorage`；`apps/ios/README.md` 的 Lookups 目錄註解補上對帳狀態、rename cascade 段落改為經 delegate。驗證：`grep -rn "renameRequested\|paymentMethodEditSucceeded\|withIsolatedCatalog" apps/ios/README.md apps/ios/CLAUDE.md .claude/rules` 無輸出；依根 `CLAUDE.md` 文件同步鐵則逐列檢查 `git status --short`，結論寫在本 task 下方 [after: 7.1]
  - 文件同步鐵則核對 (依本輪 `git status --short` 的變更路徑逐項分類)：
    - 新增或調整的硬規則、gotcha 與慣例：有影響，已同步。`apps/ios/CLAUDE.md`、`.claude/rules/ios-navigation.md`、`.claude/rules/ios-unit-tests.md` 與 `.claude/rules/ios-data-layer.md` 已涵蓋 Feature 拆分、sheet 關閉後呈現 alert、`@Shared` 斷言時機、主檔改名與付款方式更正流程。
    - 技術棧、外部服務、API 與環境設定：確認無文件影響。`apps/ios/BuyLedger.xcodeproj/project.pbxproj` 的差異只有 `CURRENT_PROJECT_VERSION` 從 356 遞增至 359；UI 測試啟動旗標只控制測試替身，不改 App 執行環境。
    - 目錄結構、feature 模組、build 與 test 指令：有影響，已同步。`Features/Lookups/Components/` 新增 `LookupItemRow` 與 `LookupItemList`，`BuyLedgerUITests/Tests/More/` 新增主檔管理流程，均已列入 `apps/ios/README.md` 的目錄樹；其餘新型別與測試檔位於既有目錄，沒有新增 build 或 test 指令。
    - 讓既有規則或描述失效的行為變更：有影響，已同步。主檔 cascade 與付款方式更正流程已更新於 `.claude/rules/ios-data-layer.md` 和 `apps/ios/README.md`；sheet 後續 alert 與 iPad 側邊欄清除「更多」路徑已記於 `.claude/rules/ios-navigation.md`。
    - `RootSidebarLayout` 的 iPad 切分頁行為：確認無文件影響。README 的平台導覽段落只概述 `NavigationSplitView` 與 `TabView`；路徑清除屬導航規則，已記於 `.claude/rules/ios-navigation.md`，不在 README 重複細節。
    - 其餘 `git status --short` 路徑：確認無文件影響。`BLUITestConfiguration`、`BLUITestDependencyOverrides`、`LaunchOptions`、`BLAccessibilityID`、`Features/Lookups/**` 內既有目錄的型別、`Features/App/RootFeature.swift`、`BuyLedgerTests/**` (含 snapshot 基準圖)、UI 測試檔案 (不含已同步的 `Tests/More/` 目錄) 與本 change 檔案只改實作、測試或驗收記錄，未改變開發環境設定或測試指令。

## 8. 驗收

- [x] 8.1 依「判讀級 findings 的逐筆處置」填完 `findings.md` 302 筆結論：修掉的項目寫 `已修正` 與負責的 task，預填的 17 筆不改動。驗證：`grep -c "結論：待填" openspec/changes/lookups-style-compliance/findings.md` 為 0 [after: 7.2]
- [x] 8.2 機械與分區檢查 (design 驗收條件 1 至 3)：以 design 指定的 `lookups_checks.py` 檢查，A 組整檔、B 組只查 `git diff` 改到的行，行寬 (python3 按字元)、尾隨空白與 tab、檔頭第 5 行完整比對、300 行上限、MARK 段名與頂層順序與重複、**區塊內容相符**、modifier 跨組順序、缺 `///`、`@Test` 的 doc 與三段標記 (When／Then 底下第一個非空行不是 `}` 或另一個標記)、`switch` 的 `case` 之間空行 (`SWC`，使用者 2026-09-24 裁決) 全部 0 筆；尾隨空白另以 `git diff --check HEAD` 與「未追蹤檔逐一 `grep -nE '[[:space:]]+$'`」兩條命令確認無輸出，涵蓋使用者自行修改的部分與腳本清單外的檔案；另逐檔閱讀 A 組至少一半的檔案對照 `formatting.md` 與 `coding-style.md` 的常見錯誤檢查清單。驗證：腳本輸出 `PASS` 且全文 (含 `WSTR` 各行的說明) 與閱讀的檔案清單記在本 task 下方 [after: 8.1]
  - 收尾記錄 (reviewer 複驗通過)：`lookups_checks.py` (無參數，41 檔) 輸出 `TOTAL {'WCL': 12} files 41 PASS`；`WCL` 是 formatting.md「Closure 與 Trailing Closure」步驟 3 允許的 closure 簽章超過 100 字元 (左側 `$0[XxxRepository.self].xxx =` 過長)，逐行：L199 WCL closure 簽章行寬 114 (formatting 允許，不計入失敗);L207 WCL closure 簽章行寬 103 (formatting 允許，不計入失敗);L210 WCL closure 簽章行寬 106 (formatting 允許，不計入失敗);L227 WCL closure 簽章行寬 103 (formatting 允許，不計入失敗);L241 WCL closure 簽章行寬 111 (formatting 允許，不計入失敗);L248 WCL closure 簽章行寬 122 (formatting 允許，不計入失敗);L60 WCL closure 簽章行寬 123 (formatting 允許，不計入失敗);L219 WCL closure 簽章行寬 127 (formatting 允許，不計入失敗);L107 WCL closure 簽章行寬 107 (formatting 允許，不計入失敗);L301 WCL closure 簽章行寬 127 (formatting 允許，不計入失敗);L466 WCL closure 簽章行寬 119 (formatting 允許，不計入失敗);L754 WCL closure 簽章行寬 123 (formatting 允許，不計入失敗)。`git diff --check HEAD` 無輸出；未追蹤檔 (排除 png) 逐一 `grep -nE '[[:space:]]+$'` 無命中。閱讀：A 組 30 檔全數由兩位獨立審查員逐檔閱讀 (1.x 規則一輪、ios-dev-kit 2.0.0 差異一輪)，清單見 `scratchpad/a-group.txt`；兩輪共約 110 項已於第 14、15 批修正，checker 依審查結果補 `MKIN`／`CLP`／`TIMP`／`CLB`／`GWTN`／`CL0`／`EQBR` 七條規則。修正後單元 127／127 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T17-06-49-418Z_pid57660_7e6daf43.xcresult`)；排序變異 (`inserting` 拿掉排序) 讓 `addingNameOnlyKindInsertsSorted` 轉紅 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T16-28-43-992Z_pid23652_1153a7c9.xcresult`，還原後 1／1)；`formSheetDismissed` 變異兩條轉紅 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T17-08-07-375Z_pid59015_19b7c576.xcresult`，還原後 2／2)；iPhone `LookupManagementTests` 七條 + `OrderDetailTests` 11／11 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T17-09-26-715Z_pid60228_2f1d4b00.xcresult`)、iPad 鍵盤預檢 2／2 後同組 11／11 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T17-18-46-194Z_pid67416_58243e96.xcresult`)。
- [x] 8.3 殘留名稱檢查 (design 驗收條件 4)：`grep -rn` 限 design 驗收條件 4 列出的本 change 範圍 (Lookups 目錄、`RootFeature.swift` 與本 change 的四類測試檔)，design 列出的舊名稱全部無輸出，`Features/Lookups/` 內 `import SwiftUI` 只出現在 View 檔。驗證：命令與輸出記在本 task 下方 [after: 8.2]
  - 收尾記錄：`grep -rnE "paymentMethodIsCardless|paymentMethodIsBankTransfer|paymentMethodIsCashOnDelivery|uniqueKeysWithValues|renameRequested|paymentMethodEditSucceeded|LookupManagementDestination|deletionConfirmation|retroactiveConfirmation:|writeFailureAlert"` 限 design 驗收條件 4 的範圍：無輸出 (exit 1)；`Features/Lookups/` 內 `import SwiftUI` 只在 `LookupNameEditorSheet.swift`、`LookupManagementView.swift`、`Components/LookupItemRow.swift`、`Components/LookupItemList.swift` 四個 View 檔；`addAlertTitle|addAlertMessage|addFieldPlaceholder` 在 `Features/Lookups/` 無輸出 (exit 1)。
- [x] 8.4 完整單元回歸 (design 驗收條件 6)：確認最後一次 Swift 寫入 (含變異驗證的還原) 的時間，鎖淺色外觀後以 `BuyLedger.xctestplan` 跑完整單元測試。驗證：xcresult 檔名時間 (UTC) 晚於最後一次 Swift 寫入 (以 `find -newermt` 核對)；失敗清單是基準的子集；頂層 `totalTestCount` 與基準的差額逐項說明，design 驗收條件 6 列出的類別都有執行且 0 失敗；已知雜訊依規則單獨重跑轉綠、未重錄基準圖；數字與 bundle 路徑記在本 task 下方 [after: 8.3]
  - 收尾記錄：最後一次 Swift 寫入是 2026-09-25 01:08:44 (+0800，即 17:08:44Z，`formSheetDismissed` 變異還原後的 `LookupManagementFeature.swift`)，以 `find apps/ios -name '*.swift' -newermt` 核對。鎖淺色外觀後以 `--scheme BuyLedger` (`BuyLedger.xctestplan`) 跑完整單元：`totalTestCount` 769、通過 767、失敗 2；bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T17-25-52-893Z_pid73158_b87153ef.xcresult` (17:25:52Z，晚於最後寫入)。兩條失敗都在 `ios-unit-tests.md` 的已知 snapshot 雜訊清單：`orderEditViewMergeContextBaseline()` 單獨重跑通過 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T18-35-56-444Z_pid23560_5d3feb25.xcresult`)；`quoteViewBaseline()` 單獨連跑兩次皆 1／1 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T18-40-25-550Z_pid28039_48511546.xcresult`、`/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T18-41-42-160Z_pid29722_83be53bd.xcresult`)；判定渲染雜訊，未重錄基準圖。與 0.2 基準 (752) 的差額 +17 = 移除 27、新增 44，逐項比對兩份 bundle 的測試清單：移除為舊 `LookupManagementFeatureTests` 23 條 (改寫進拆分檔，更正流程移到 `PaymentMethodCorrectionFeatureTests`) 與 `RootFeatureTests` 4 條 `...InOneReducerCall` (改名為 `...RewritesOrdersThroughDelegate`)；新增為 `LookupManagementFeatureTests` 各拆分檔 22 條、`PaymentMethodCorrectionFeatureTests` 8 條、`RootFeatureTests` 8 條 (4 條改名、改名寫入失敗 1 條、側邊欄 3 條)、`SnapshotTests` 5 條 Lookups、`LookupCatalogTests` 2 條、`BLUITestConfigurationTests` 1 條；其餘類別與基準完全相同。design 驗收條件 6 列出的類別皆有執行且 0 失敗。
- [x] 8.5 UI 回歸 (design 驗收條件 7)：iPhone 與 iPad 各跑一次 `BuyLedgerUITests` 主回歸，iPad 前確認軟體鍵盤可顯示。驗證：逐台比較 0.2 基準，頂層 `totalTestCount` 等於基準加 7、失敗清單是基準失敗清單的子集，且 `LookupManagementTests` 七條與 `OrderDetailTests` 全綠；bundle 時間晚於最後一次 Swift 寫入；數字與路徑記在本 task 下方 [after: 8.4]
  - 收尾記錄：iPhone (`DDAA3311-B464-4DD3-96B8-360B26AF1929`) 主回歸 `totalTestCount` 71 (基準 64 + 7)、通過 70、失敗 1：`OrderDetailTests/testCashOnDeliveryCorrectionPersistsAfterRelaunch()` 的 `Activation point invalid` (與 0.2 基準 iPad 同一條、同一類 hittability 雜訊)，單獨重跑 1／1 通過 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T18-37-15-594Z_pid25203_13fae58b.xcresult`)；主回歸 bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T17-27-16-231Z_pid74972_37821dd4.xcresult`。iPad (`6B65ED1C-3C2E-42BA-B1E7-08F606F175C5`) 鍵盤預檢 `KeyboardDismissTests` 2／2 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T18-03-45-628Z_pid409_72373fd9.xcresult`)，主回歸 `totalTestCount` 71 (基準 64 + 7)、通過 71、失敗 0 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-24T18-05-15-177Z_pid1693_8d427d60.xcresult`)。兩台 `LookupManagementTests` 七條與 `OrderDetailTests` 皆綠 (iPhone 的雜訊條單獨重跑綠)；bundle 時間皆晚於最後一次 Swift 寫入 (17:08:44Z)。
- [x] 8.6 以 `spectra validate lookups-style-compliance` 確認 change 文件一致，並以 `git status --porcelain` 確認所有變動都在 design「範圍邊界」內 (不整體 `.strip()` 輸出)。驗證：validate 通過；範圍外檔案 0 個，`project.pbxproj` 只有 `CURRENT_PROJECT_VERSION` 的遞增 [after: 8.5]
  - 收尾記錄：`spectra validate lookups-style-compliance` 輸出 `✓ lookups-style-compliance — valid`。`git status --porcelain -uall` 逐列 (不整體 strip) 共 59 個異動路徑，比對 proposal Impact 清單 (含 `__Snapshots__/SnapshotTests+Lookups/` 基準圖) 與 design「範圍邊界」的連帶檔案，範圍外 0 個。`project.pbxproj` 的差異只有 6 處 `CURRENT_PROJECT_VERSION = 356` → `359` (agvtool 遞增)。

## 9. verify／review 後的修正 (使用者 2026-09-25 裁決)

- [x] 9.1 依 design「改名是單一交易」把主檔改名改成單一交易：`OrderPersistence` 新增四個 `apply…Rename`，主檔改名規則抽成共用 helper，`OrderRepository` 以交易 closure 取代只改訂單的改名 closure，`LookupItemOperations.renameItem` 刪除補償；持久層測試涵蓋 lookup-management spec「A rename writes the stored lookup and the stored orders in one transaction」的兩個範例 (失敗表兩列、撞名成功) 與付款方式旗標合併，另有「別的 context 改過旗標後再經交易合併不遺失」與「改名失敗後再存一筆無關訂單，主檔表仍維持原狀」兩條；交易用 `OrderPersistence` 內的一次性 context (使用者 2026-09-25 裁決)；reducer 與 `RootFeatureTests` 的改名測試改為注入交易 closure；`.claude/rules/ios-data-layer.md` 補「主檔改名是一次原子操作」。驗證：相關單元測試全綠；兩次變異 (交易內不改主檔記錄、改回使用長命 `modelContext`) 各自讓對應持久層測試轉紅，還原後轉綠 [after: 8.6]
  - 收尾記錄 (reviewer 複驗通過)：第一版把交易放在長命 `modelContext` 內，`renameFailureRollsBack` 兩列的同實例讀回都殘留 (`["服飾"]` 讀到 `["衣著"]`、`["服飾", "衣著"]` 讀到 `["衣著"]`，log 有 `ModelActor was deinitialized while ModelContext has unsaved changes`)，證實 save 失敗後 `rollback()` 還原不了插入與刪除；使用者裁決改用 `OrderPersistence` 內的一次性 context。Claude 代修 8 個傳入交易的 closure 補 `() throws(PersistenceError) in`。修正後 `OrderPersistenceTests`、`NameLookupPersistenceTests`、`PaymentMethodPersistenceTests`、`LookupManagementFeatureTests`、`PaymentMethodCorrectionFeatureTests`、`RootFeatureTests`、`LayerBoundaryTests`、`TestSuiteIntegrityTests` 與六條 Lookups snapshot 共 146 條全綠 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-09-26-475Z_pid97207_e7355a09.xcresult`)。變異 (a) 交易跳過主檔改名：`categoryRenameTransactionUpdatesLookupAndOrders` 1／1 轉紅 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-10-34-803Z_pid98388_86137aeb.xcresult`)；(b) 改回長命 `modelContext`：`failedRenameDoesNotPersistLookupWhenNextOrderIsCreated` 與 `renameFailureRollsBack` 2／2 轉紅 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-11-10-761Z_pid98962_3139c390.xcresult`)；還原後 SHA-256 `5839f2cf040b07ebf68686d7d6a4fae6e01e41a7c8520d2f54f555eacc55091a` 一致，重跑 4／4 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-12-26-345Z_pid289_22e97a64.xcresult`)。`paymentMethodRenameUsesFlagsSavedByAnotherContext` 的原失敗是 `.none` 被解析成 `Optional.none` 的測試錯誤，跨 context 旗標保留成立。
- [x] 9.2 依 design「寫入失敗改為一次性提示」的裁決，付款方式更正成功不清除 `hasLoadFailed`，補一條測試：首次載入失敗後更正成功，`hasLoadFailed` 仍為 `true`。驗證：該測試全綠，變異 (恢復清除) 讓它轉紅 [after: 8.6]
  - 收尾記錄 (reviewer 複驗通過)：`correctionSuccessKeepsTheInitialLoadFailureState` 在同一 146 條 bundle 內通過；變異 (恢復 `state.hasLoadFailed = false`) 讓它 1／1 轉紅 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-11-42-893Z_pid99459_78be8130.xcresult`)，還原後 SHA-256 `0a12765c653ad90f6c246bd5ef7ee7aaa27d026cf0ee6c3d8e0301fd402d44d4` 一致，重跑通過 (同上 4／4 bundle)。
- [x] 9.3 verify／review 的例行項目：MoreView 找不到管理頁 store 時的替代畫面補 snapshot (新增基準圖，不重錄既有)；`+AlertTiming` 的立即失敗測試改用 spec 範例數值 (目錄 ["服飾"]、送出「失敗類別」、斷言 `items == ["服飾"]`)；補同名付款方式的畫面層測試 (目錄有兩筆「匯款」旗標不同時，`editButtonTapped(name: "匯款")` 帶入第一筆旗標)；`.claude/rules/ios-navigation.md`「表單儲存前的同步驗證由父層決定是否關閉」改寫為現行寫法 (子層能自行判斷的驗證由子層做、通過才送 delegate，需要父層資料的驗證由父層決定是否關閉)。驗證：新增測試全綠；新 snapshot 錄製後以方法層重跑通過 [after: 8.6]
  - 收尾記錄 (reviewer 複驗通過)：`moreViewMissingLookupManagementShowsUnavailableBaseline` 首次錄製後目視確認 (推入後的替代畫面：返回鍵、警告圖示、「無法載入主檔管理頁面，請稍後再試。」)，方法層重跑通過；`+AlertTiming` 立即失敗測試改用 spec 範例數值；同名付款方式畫面層測試通過；`ios-navigation.md` 表單驗證規則已改寫為現行寫法。以上皆在 146 條 bundle 內全綠。
- [x] 9.4 修正後的完整驗收：checker、`git diff --check HEAD` 與未追蹤檔尾隨空白、完整單元回歸 (失敗 ⊆ 基準與已知雜訊)、兩台 UI 主回歸 (`totalTestCount` 基準加 7)、design「reducer 不 import SwiftUI」要求記在 3.1 的 `nm -u … | swift demangle | grep 'TextState.init'` 補錄於本 task 下方、`spectra validate` 與範圍檢查；數字與 bundle 路徑記在本 task 下方 [after: 9.3]
  - 收尾記錄：最後一次 Swift 寫入 2026-09-25 11:12:15 (+0800，即 03:12:15Z，9.2 變異還原)。checker 48 檔 `TOTAL {'WCL': 12} files 48 PASS`；`git diff --check HEAD` 與未追蹤檔尾隨空白皆無輸出。完整單元 (`BuyLedger.xctestplan`) `totalTestCount` 778、通過 777、失敗 1 (`ordersCompactViewMultiSelectBaseline()`，`ios-unit-tests.md` 已知 snapshot 雜訊，單獨重跑 1／1，bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T04-23-37-298Z_pid51792_4052ffc3.xcresult`)；主 bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-13-34-282Z_pid1881_fc5fd643.xcresult`。相對 8.4 的 769：移除 2 條補償測試、新增 11 條 (9.1 的 4 條 reducer／持久層改名測試與 6 條 `OrderPersistenceTests` 改名交易、9.2 的 1 條、9.3 的同名付款方式與 MoreView snapshot)，逐項比對兩份 bundle 的測試清單確認。UI 主回歸：iPhone `totalTestCount` 71 (基準 64 + 7) 全過 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-14-09-823Z_pid2757_c3d23529.xcresult`)；iPad 鍵盤預檢 2／2 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-51-22-862Z_pid28639_d0b05ff3.xcresult`) 後主回歸 71 全過 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T03-52-59-324Z_pid29880_4d91c524.xcresult`)。`nm -u LookupManagementFeature+Destination.o | swift demangle | grep 'TextState.init'` 只有 `SwiftNavigation.TextState.init(Foundation.LocalizedStringResource) -> SwiftNavigation.TextState` (該 `.o` 建置時間晚於原始檔最後修改)。`spectra validate` 通過；`git status --porcelain -uall` 67 個路徑，範圍外 0 個；`project.pbxproj` 只有 6 處 `CURRENT_PROJECT_VERSION` 356 → 359。
- [x] 9.5 第二輪 verify／review 的修正：`LookupRecordRenamer` 移到獨立檔 `Core/Persistence/LookupRecordRenamer.swift` (比照 `OrdersFilterOperations`：本體空 enum、方法放 `// MARK: - Internal Method` extension)；改名交易補「長命 `OrderPersistence` 先讀過訂單、交易後再讀到新名稱」守門測試，`.claude/rules/ios-data-layer.md` 的非長命 context 寫 `OrderRecord` 例外清單補上四個 `apply…Rename`；`RootFeatureTests.categoryRenameCascadesInsideMultiCategoryOrders` 補單一類別 `["美妝"]` 的訂單並斷言改名後為 `["彩妝保養"]`；`State.classification(for:)` 補參數化測試 (付款方式同名取第一筆旗標、商品類別回 `nil`)；刪除與 `+Forms`／`+Failures` 重複的 `LookupManagementFeatureTests+Rename.swift`，把「交易 closure 只呼叫一次且帶 (舊名, 新名)」併進保留的測試，`+Failures` 的改名失敗說明改為單一交易；`OrderPersistenceTests.swift` 的 Private Method 改回 `private extension` 並拿掉逐一的 `private`，`+LookupRename` 的範例型別移進 Nested Types、拿掉多餘的 `private`，`extension OrderPersistence` 的測試讀取 helper 移到獨立檔；`LookupItemList` 的 Private Views 依屬性在前、呼叫者在前排序；`BLUITestDependencyOverrides` 的訂單 repository 覆寫改為以參數傳入，不依賴呼叫順序。驗證：checker (含新增的 `TOP`／`PRIV`) PASS；完整單元回歸 (失敗 ⊆ 已知雜訊)、兩台 UI 主回歸 (基準加 7)；新守門測試以變異確認 (交易成功後不讓長命 context 看到新值的情況無法直接製造時，改以讀回斷言與 PaymentMethodPersistenceTests 同型寫法為準) [after: 9.4]
  - 收尾記錄 (reviewer 複驗通過)：第 18 批完成本 task 列出的項目；相關單元 157 條全綠 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T05-48-44-456Z_pid16827_df7fc683.xcresult`)。長命 context 守門以變異驗證：交易跳過改寫訂單時，`categoryRenameTransactionUpdatesLookupAndOrders` 在長命 `OrderPersistence` 重讀的斷言轉紅 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T05-50-05-399Z_pid18138_3d907f90.xcresult`)，還原後 SHA-256 `5839f2cf040b07ebf68686d7d6a4fae6e01e41a7c8520d2f54f555eacc55091a` 一致、重跑通過。使用者另指出 `OrderRepository` 的 Internal Method 違反 ios-dev-kit 2.0.0 closure 規則 (B 組只查 diff 行而漏檢)：`live(...)` 整串 closure 改為只補 `throws(E)` 的短式、簽章與 `{` 同行，`live` 的 doc 補摘要後空行；依使用者要求一併修正同型既有違規 (`BLUITestDependencyOverrides` 15 處、`RootFeatureTests` 2 處、`LocalizationCatalogTests` 1 處)，`LocalizationCatalogTests` 的 `compactMap { key, value -> String? in` 因編譯器無法推斷 `ElementOfResult` 保留標註；checker 補 `CLA`／`TOP`／`PRIV`。最終 checker 49 檔 `TOTAL {'WCL': 15} files 49 PASS` (`WCL` 為 formatting 允許的長 closure 簽章)，`git diff --check HEAD` 無輸出。最後一次 Swift 寫入 2026-09-25 15:11:57 (+0800，即 07:11:57Z)。完整單元 `totalTestCount` 777、通過 775、失敗 2 (`orderEditViewBaseline()`、`quoteViewBaseline()`，皆為已知 snapshot 雜訊，單獨重跑各 1／1：bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T08-23-09-338Z_pid45187_64985f35.xcresult`、`/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T08-24-25-049Z_pid46945_9e3486ed.xcresult`)；主 bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T07-13-17-696Z_pid86502_cc950222.xcresult`。相對 9.4 的 778：刪除重複的 `+Rename` 兩條、新增 `classificationMatchesExample(example:)` 一條。UI：iPhone 71 全過 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T07-14-05-398Z_pid87331_416bcd01.xcresult`)；iPad 鍵盤預檢 2／2 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T07-50-59-927Z_pid18160_8a40b390.xcresult`) 後 71 全過 (bundle `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-25T07-52-39-549Z_pid19617_0d56ba6c.xcresult`)。`spectra validate` 通過；異動 68 個路徑、範圍外 0；`project.pbxproj` 只有 6 處 `CURRENT_PROJECT_VERSION` 356 → 359。

