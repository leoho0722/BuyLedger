<!--
逐檔的審查項目清單見本目錄的 findings.md，實作每個檔案時以它決定要修哪幾項。
可改檔案 allowlist 見 design.md 的 Implementation Contract，清單以外的檔案一律不動。
機械檢查的三個命令見 design.md「機械規則只修本步動到的檔案」，每個 task 執行時把檔案集合換成該 task 負責的清單，命令與輸出記在本檔。
-->

## 0. 開工前基準

- [x] 0.1 取得本 change 的測試基準：在第一次修改 allowlist 內的實作檔案之前跑一次完整單元測試，把執行的測試數、通過數與失敗清單記在本檔，作為完工時比較的 baseline。驗證：baseline 數字與執行時間寫進本檔，且執行時間早於第一次實作檔案寫入。
  - 實際執行時間：2026-09-19 00:09:58 +08:00 開始；測試程序回報 duration `117592 ms`，早於本 task 後續任何實作檔寫入。
  - 命令：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`
  - 實際結果：status `FAILED`；discovered `709`、passed `704`、failed `3`、skipped `0`。
  - 失敗清單：`SnapshotTests/orderEditViewBaseline()`、`SnapshotTests/ordersCompactViewMultiSelectBaseline()`、`SnapshotTests/quoteViewBaseline()`；三者皆為 snapshot baseline mismatch。
  - 確認結論：完整 unit-test baseline 已實際執行並記錄；本 change 開工前既有 3 項 snapshot 失敗，非本 change 造成。
  - Review 後完整回歸：2026-09-19 01:04:41 +08:00 開始；同一命令且未帶 `-only-testing`，測試程序回報 duration `61355 ms`；status `FAILED`；XcodeBuildMCP 回報 discovered `712`、passed `708`、failed `2`、skipped `0`。
  - 本次失敗清單：`SnapshotTests/orderEditViewBaseline()`、`SnapshotTests/quoteViewBaseline()`；`SnapshotTests/ordersCompactViewMultiSelectBaseline()` 已通過。原始 xcresult 的 `totalTestCount` 為 `710`、passed `708`、failed `2`、skipped `0`，build log 亦記錄 `Test run with 710 tests in 64 suites`。XcodeBuildMCP 未提供原始 xcresult 計數，故以 `xcresulttool` 核對口徑。
  - 與 baseline 比較：原始 XCTest 實際執行數由 `707` 增至 `710`，passed 由 `704` 增至 `708`，failed 由 `3` 降至 `2`，skipped 維持 `0`；本次沒有新增非 baseline 的失敗。
  - baseline 計數差異說明：baseline 的 XcodeBuildMCP `discovered 709` 與 `passed 704 + failed 3 + skipped 0 = 707` 相差 `2`；baseline xcresult 的 `totalTestCount` 確為 `707`，且 `skippedTests=0`、failure 清單只有上列 3 項。這 2 項不是可指名的測試、也不是被跳過的測試，而是 XcodeBuildMCP discovery 計數相對 XCTest 實際執行數多算 2；本次亦同樣是 `discovered 712` 對原始 `totalTestCount 710`。

## 1. 錯誤型別改形狀

- [x] 1.1 [after: 0.1] 依「錯誤型別以 underlying 保留原始錯誤並移除 Equatable」，讓 `PersistenceError` 的三個 case 改帶 `underlying: any Error & Sendable`、型別不再遵循 `Equatable` 與 `LocalizedError`，並讓 `mapFetch` / `mapSave` / `mapContainerCreation` 以 `error as NSError` 橋接後帶入 (泛型參數改具名 `Value`、只服務單一型別的 `mapContainerCreation` 改非泛型)。滿足 Requirement: Persistence storage failures use a shared typed error。驗證：新增單元測試，讓 fetch 與 save 失敗後取出的 underlying 其 `domain`、`code` 與 `localizedDescription` 等於替身拋出錯誤橋接後的值 (不斷言物件身分)，且專案可編譯。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/PersistenceErrorTests" --output json --progress false`；status `SUCCEEDED`，duration `82278 ms`，discovered `2`、passed `2`、failed `0`、skipped `0`，無 test failures。此 targeted test 同時完成專案編譯確認。
  - 最終版本重跑：同一命令於最後測試檔調整後 status `SUCCEEDED`，duration `17959 ms`，discovered `2`、passed `2`、failed `0`、skipped `0`，無 test failures。
- [x] 1.2 [after: 1.1] 讓 `OrderPersistenceError`、`PaymentMethodPersistenceError`、`CurrencyMetadataPersistenceError` 的 `storage` case 承載改形狀後的 `PersistenceError`，三個型別不再遵循 `Equatable` 與 `LocalizedError`，語意 case 的識別值照舊，簽章宣告的錯誤型別不變。滿足 Requirement: Domain persistence errors preserve semantic failures 與 Requirement: Persistence boundaries use complete typed throws contracts。驗證：既有 `OrderPersistenceTests` 與 `PaymentMethodPersistenceTests` 全綠，重複訂單編號與找不到訂單的斷言改以 `if case` 比對後仍然通過。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/OrderPersistenceTests" --output json --progress false`；status `SUCCEEDED`，duration `25926 ms`，discovered `39`、passed `39`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/PaymentMethodPersistenceTests" --output json --progress false`；status `SUCCEEDED`，duration `17462 ms`，discovered `8`、passed `8`、failed `0`、skipped `0`。
- [x] 1.3 [after: 1.1] 讓 `PersistenceRecoveryError` 的三個 case 改帶 `underlying: any Error & Sendable`、不再遵循 `Equatable`，但保留 `LocalizedError` (因為 `PersistenceFailureFeature` 以無型別 catch 取 `localizedDescription` 顯示)，顯示文字仍是原始錯誤的 localized description。滿足 Requirement: Store quarantine uses a separate typed recovery error。驗證：`PersistenceRecoveryTests` 與 `PersistenceFailureFeatureTests` 全綠，並新增一條測試確認 `errorDescription` 等於來源錯誤的 localized description。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/PersistenceRecoveryTests" --output json --progress false`；status `SUCCEEDED`，duration `19295 ms`，discovered `9`、passed `9`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/PersistenceFailureFeatureTests" --output json --progress false`；status `SUCCEEDED`，duration `17217 ms`，discovered `4`、passed `4`、failed `0`、skipped `0`。
- [x] 1.4 [after: 1.1] 讓 `APIError` 的 transport 與 decoding case 改帶 `underlying: any Error & Sendable` (以 `error as NSError` 橋接)、型別不再遵循 `Equatable`，其餘分類 case 形狀不變；同步調整 `OllamaClient`、`FxFeature`、`QuoteFeature`、`AISummaryFeature` 與 `BLUITestDependencyOverrides` 的建構處，使用者看到的訊息仍由 Feature 自己的 message mapping 產生且不含網址或標頭值。滿足 Requirement: Networking errors carry the source failure without exposing it to the user。驗證：`HTTPClientTests` 與 `ExchangeRateClientTests` 中 3 處以 `==` 比對 `APIError` 的斷言改以 `if case` 後全綠，並新增一條測試確認 transport case 的 underlying 帶得到來源錯誤的 domain 與 code。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/HTTPClientTests" --output json --progress false`；status `SUCCEEDED`，duration `17494 ms`，discovered `3`、passed `3`、failed `0`、skipped `0`；transport forwarding 測試實際驗證 underlying 的 domain、code、localizedDescription。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/ExchangeRateClientTests" --output json --progress false`；status `SUCCEEDED`，duration `16518 ms`，discovered `9`、passed `9`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/APIErrorMappingTests" --output json --progress false`；status `SUCCEEDED`，duration `16952 ms`，discovered `4`、passed `4`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/OllamaClientTests" --output json --progress false`；status `SUCCEEDED`，duration `17412 ms`，discovered `7`、passed `7`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/AISummaryFeatureTests" --output json --progress false`；status `SUCCEEDED`，duration `17647 ms`，discovered `6`、passed `6`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/QuoteFeatureTests" --output json --progress false`；status `SUCCEEDED`，duration `16831 ms`，discovered `19`、passed `19`、failed `0`、skipped `0`。
- [x] 1.5 [after: 1.4] 讓 `CurrencyMetadataRepositoryError` 不再遵循 `Equatable` 與 `LocalizedError` (兩個 case `api` 與 `persistence` 只轉送，不新增 underlying)，並讓 `CalendarReminderError` 的 `system(message:)` 改為 `system(underlying:)`、型別不再遵循 `Equatable`，`eventIdentifierMissing` 與 `noWritableCalendar` 形狀不變；移除 `LocalizedError` 前逐一確認沒有畫面讀取其 `errorDescription`，確認結果寫進本檔。同時處理 `CampaignFeature` 以 `catch CalendarReminderError.noWritableCalendar` 比對的那一處，若移除 `Equatable` 後編不過就改寫成型別化 catch 內的 `if case`。滿足 Requirement: Currency metadata preserves cache and API error categories。驗證：`CurrencyMetadataCacheTests`、`CalendarReminderTests`、`CampaignFeatureTests` 與 `CampaignReminderFailureTests` 改以新形狀建構與 `if case` 比對後全綠。
  - 確認結果：執行 `rg -n 'CurrencyMetadataRepositoryError.*(errorDescription|localizedDescription)|(errorDescription|localizedDescription).*CurrencyMetadataRepositoryError' apps/ios/BuyLedger/Features apps/ios/BuyLedger/Core || true` 無輸出；未找到畫面讀取 `CurrencyMetadataRepositoryError` 的 `errorDescription` 或 `localizedDescription`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/CurrencyMetadataCacheTests" --output json --progress false`；status `SUCCEEDED`，duration `18281 ms`，discovered `4`、passed `4`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/CalendarReminderTests" --output json --progress false`；status `SUCCEEDED`，duration `16840 ms`，discovered `2`、passed `2`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/CampaignFeatureTests" --output json --progress false`；status `SUCCEEDED`，duration `18192 ms`，discovered `34`、passed `34`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/CampaignReminderFailureTests" --output json --progress false`；status `SUCCEEDED`，duration `16770 ms`，discovered `11`、passed `11`、failed `0`、skipped `0`。
  - Review 後修正驗證：`PersistenceErrorTests` 兩處改為單一通用 `catch` 內的 exhaustive `switch`；`CurrencyMetadataCacheTests` 與 `HTTPClientTests` 的本輪新增 `Issue.record` 訊息均為中文，且 typed throws 的不可達第二個 `catch` 已移除；`OrderPersistenceTests` 與 `PaymentMethodPersistenceTests` 的多餘 typed-error `as` 也已移除。`CampaignFeature` 的 `CalendarReminderError` catch 已以三個 case 的 `switch` 窮舉。
  - Review 後行寬驗證：依 design.md 的 Python3 Unicode 行寬命令檢查 `CurrencyMetadataCacheTests.swift`、`ExchangeRateClientTests.swift`、`HTTPClientTests.swift`；輸出僅為既有超寬行（`ExchangeRateClientTests.swift` 15 行、`HTTPClientTests.swift` 3 行），`CurrencyMetadataCacheTests.swift` 無輸出。另以相同 `len` 規則檢查本輪 diff 新增行，無輸出；本輪指定的 7 行已全部低於 100 字元。

## 2. 靜默失敗改為可觀察的失敗

- [x] 2.1 [after: 1.2] 依「記錄解析失敗改為拋出持久化錯誤」，新增 `RecordDecodingError` (遵循 `Error` 與 `Sendable`，帶 `entity`、`identifier`、`field`、`rawValue` 四個字串)，讓 `OrderRecord` 與 `CampaignRecord` 轉領域型別的函式由不可拋出改為 `throws(PersistenceError)` (這是替原本沒有失敗通道的函式新增通道，不是換掉既有 boundary 宣告的錯誤型別)，遇到無法解析的收款狀態或開團狀態 rawValue 就拋 `PersistenceError.fetchFailed(underlying:)` 原樣帶著它 (不橋接成 `NSError`，呼叫端才能 pattern-match 四個欄位)，不再補 `.pending` 或 `.ongoing`。滿足 Requirement: Unparsable stored raw values fail the read。驗證：新增單元測試，在 in-memory container 寫入 `"legacy-status"` 這類無法解析的 rawValue 後讀取會拋出該錯誤，以 `if case` 取出 underlying 並斷言它是 `RecordDecodingError`、四個值分別是記錄型別名、該筆 id、欄位名與原始字串；另斷言所有合法 rawValue 仍轉出與改動前相同的領域值。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/RecordDecodingTests" --output json --progress false`；status `SUCCEEDED`，discovered `3`、passed `3`、failed `0`、skipped `0`；三個測試涵蓋兩種 invalid rawValue 的 `RecordDecodingError` 四欄位與四個合法 rawValue。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/OrderPersistenceTests" --output json --progress false`；status `SUCCEEDED`，discovered `39`、passed `39`、failed `0`、skipped `0`。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/CampaignPersistenceTests" --output json --progress false`；status `SUCCEEDED`，discovered `9`、passed `9`、failed `0`、skipped `0`。
- [x] 2.2 依「照片匯入回傳成功資料與失敗張數」，新增 `PhotoImportResult` (`Sendable`、`Equatable`，成員 `photos: [Data]` 與 `failedCount: Int`)，`PhotoClient.importPhotos` 改回傳它且不拋出，載入拋錯、載入回傳 nil、正規化回傳 nil 三種都計入 `failedCount`；`OrderEditFeature.State` 新增 `photoImportFailureCount`，`OrderEditView` 的 `photosSection` 在其大於 0 時多顯示一行 `"有 %lld 張照片無法匯入。"` 的輔助文字 (樣式比照同 section 既有的照片載入失敗那一行，版面結構不變)，下一批匯入開始時歸零。滿足 Requirement: Imported photos are normalized before storage。驗證：新增 TestStore 測試覆蓋三張全成功、三張中一張載入失敗、兩張全部正規化失敗三種情形，斷言草稿照片數與 `photoImportFailureCount`；新字串已加入 `Localizable.xcstrings` 並補英文。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/OrderEditFeatureTests" --output json --progress false`；status `SUCCEEDED`，discovered `44`、passed `44`、failed `0`、skipped `0`；包含三張全成功、三張中一張載入失敗、兩張全部正規化失敗的 TestStore 情境。
  - 實際驗證：以 Python JSON 語意比較確認 `Localizable.xcstrings` 共 `525` 個 key，除了新增 `"有 %lld 張照片無法匯入。"` 外的 base hash 為 `259ced9042335da72bcb2ea08956b7eec7e15e2cc30d4a9ff4eb04b2648fad2d`，`only_expected_key=True`；新增 key 同時有 `en` 與 `zh-Hant`，Xcode build 未產生其他結構字串雜訊。
  - 確認結論：inline 文字的實際顯示尚未經 UI automation 實測；本 task 僅以 TestStore 驗證 state，UI automation 留後續步驟處理。
- [x] 2.3 讓 `FxRates.toTwd` 不再以 `?? 0` 掩蓋解析失敗，改用不會產生 optional 的 `Decimal(sign:exponent:significand:)` 建構，Preview 匯率表不會因為解析失敗而變成 0。驗證：新增單元測試斷言 TWD、KRW、JPY、USD 四個幣別的匯率值分別為 1、0.0228、0.2105、32.45，並以 `grep -n '??' apps/ios/BuyLedger/Core/Domain/FxRates.swift` 無輸出確認掩蓋已移除。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/FxRatesTests" --output json --progress false`；status `SUCCEEDED`，discovered `1`、passed `1`、failed `0`、skipped `0`；通過 TWD、KRW、JPY、USD 四個值的 exact `Decimal` 斷言。
  - 實際驗證：`grep -n '??' apps/ios/BuyLedger/Core/Domain/FxRates.swift`；無輸出，exit code `1`（未找到符合項目）。
  - 第 2 組完整回歸：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`；xcodebuildmcp 回報 `discovered 718`、`passed 714`、`failed 2`、`skipped 0`，status `FAILED` 僅因既有 snapshot mismatch：`SnapshotTests/orderEditViewBaseline()`、`SnapshotTests/quoteViewBaseline()`。
  - 完整回歸同口徑核對：`xcrun xcresulttool get test-results summary --path /Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-18T17-53-50-160Z_pid77977_81b39be2.xcresult` 回報 XCTest `totalTestCount 716`、`passedTests 714`、`failedTests 2`、`skippedTests 0`。XcodeBuildMCP 未提供原始 xcresult 計數，故以 `xcresulttool` 核對口徑。相對 task 0.1 baseline `totalTestCount 707`、`passed 704`、`failed 3`、`skipped 0`：測試數 `+9`、通過數 `+10`、失敗數 `-1`；失敗清單仍是 baseline 既有的上述兩條，沒有新增失敗。

## 3. 依賴注入與 Core 邊界

- [x] 3.1 依「幣別快取的時間改走依賴注入」，讓 `CurrencyMetadataRepository.live` 的 `now` 參數保留但移除 `{ Date() }` 預設值，`liveValue` 與 `previewValue` 改傳一個宣告 `@Dependency(\.date)` 後回傳 `date.now` 的 closure (依 `apps/ios/CLAUDE.md` 的規定，不用 `@Dependency(\.date.now)`)，production 不再直接呼叫 `Date()`。驗證：既有 `CurrencyMetadataCacheTests` 三處 `now:` 注入不需修改即全綠，並以 `grep -n 'Date()' apps/ios/BuyLedger/Core/Dependencies/CurrencyMetadataRepository.swift` 無輸出確認。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/CurrencyMetadataCacheTests" --output json --progress false`；status `SUCCEEDED`，duration `49059 ms`，discovered `4`、passed `4`、failed `0`、skipped `0`。既有三處 `now:` 注入均未修改，且同類別第四個測試也通過。
  - 實際驗證：`rg -n 'Date\(\)' apps/ios/BuyLedger/Core/Dependencies/CurrencyMetadataRepository.swift`；無輸出，exit code `1`。
- [x] 3.2 依「Core 日誌分類不使用 Feature 名稱」，把 `AppLogger.Category.aiSummary` 改名為 `inference`、rawValue 由 `"AISummary"` 改為 `"Inference"`，對應的 `static var` 一併改名，呼叫端跟著改；`persistence` 分類與 subsystem 取得方式不動。驗證：專案可編譯，`grep -rn 'aiSummary' apps/ios/BuyLedger/Core/` 無輸出 (只限 Core：Feature 自己的狀態、Action 與識別字用 `aiSummary` 命名與本 task 無關，不在範圍內)，`AppLogger.inference` 的唯一呼叫端 `AISummaryFeature.swift` 已跟著改名，且模擬器主控台看得到 category 為 `Inference` 的日誌。
  - 實際驗證：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/AISummaryFeatureTests" --output json --progress false`；status `SUCCEEDED`，duration `39505 ms`，discovered `6`、passed `6`、failed `0`、skipped `0`，完成專案編譯與 AISummaryFeature 行為確認。
  - 實際驗證：`rg -n '\baiSummary\b' apps/ios/BuyLedger/Core/`；無輸出，exit code `1`。
  - 實際驗證：`rg -n 'AppLogger\.inference' apps/ios/BuyLedger/Features/AISummary/AISummaryFeature.swift`；輸出 `AISummaryFeature.swift:134`，Core 內 `AppLogger.aiSummary` 定義與呼叫均已移除。
  - 實際驗證：上述 targeted test 的 build log `/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/logs/test_sim_2026-09-18T18-22-12-095Z_pid499_c7132790.log` 第 1845 行輸出 `[Inference] AI 總結無法啟動：OLLAMA_API_KEY 未注入 (xcconfig 未設定)`，確認模擬器主控台 category 為 `Inference`。

## 4. codegen 模板

- [x] 4.1 依「codegen 模板本步只改 doc 與 protocol 位置」，讓 `datamodel-gen` 產出的 Swift 採用 design 表格定案的三句固定 doc 文字 (initializer、wrapper 的 rawValue、identity 的 id)、補充段落改為 `- Note:`、identity enum 與 wrapper 的 protocol 遵循連同其 `id` 一起寫在帶 MARK 分區的 extension；schema 帶來的 doc 字串照舊原樣輸出，不改任何 `shared/data-model/schema/` 的 YAML，`$newUUID` 預設值依「本 change 接受的規範例外」保持不變。滿足 Requirement: Generated Swift owns the data shape and handwritten extensions own behavior。驗證：在 `shared/data-model/generator` 執行 `bun test` 通過，全部 10 個 Swift golden fixture 與新輸出逐字一致，且 `git status --short shared/data-model/schema/` 無輸出。
  - 實際驗證：在 `shared/data-model/generator` 執行 `bun test`；exit code `0`，`52 pass`、`0 fail`、`194 expect() calls`，fixture 三語言 golden 逐 byte 比對通過。
  - 實際驗證：以 `bun -e` 呼叫 `planGeneration` 逐一比較 `shared/data-model/fixtures/expected/swift/` 全部 `10` 個檔案；輸出 `Swift fixtures checked: 10; drifted: 0`，exit code `0`。本次實際內容變更為 `SampleStatus`、`SampleTag`、`SampleOrder`、`SampleReceipt`、`SampleSequence` 五個 golden。
  - 範圍確認：修改前後 `shared/data-model/schema/` 12 個 YAML 的 SHA-1 完全相同；Kotlin 與 TypeScript golden 共 20 個檔案的 SHA-1 完全相同。依本輪「不碰任何 Git」要求，未執行 task 原文的 `git status`，改以內容 hash 驗證 schema 未變更且非 Swift golden 未變更。
- [x] 4.2 [after: 4.1] 重新產生所有平台輸出並提交生成檔，使 `apps/ios/BuyLedger/Core/Domain/Generated/` 的 12 個檔案與 schema 無漂移，iOS 專案在新生成檔下可編譯。驗證：在 `shared/data-model/generator` 依序執行 `bun run unlock`、`bun run generate`、`bun run check`，`check` 退出碼為 0；iOS 與 iPadOS 兩個目的地各完成一次 build 且 `BuyLedgerTests` 全數通過。
  - 實際驗證：`bun run unlock && bun run generate && bun run check`；依序解鎖 `12` 個生成檔、產出 `12` 個檔案，`check 通過：磁碟產出與 schema 同步`，整體 exit code `0`。
  - 實際驗證：以 `&&` 序列化執行 `xcodebuildmcp --log-level error simulator build --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-id DDAA3311-B464-4DD3-96B8-360B26AF1929 --output json && xcodebuildmcp --log-level error simulator build --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-id 6B65ED1C-3C2E-42BA-B1E7-08F606F175C5 --output json`；iOS `iPhone 17` status `SUCCEEDED`、duration `55477 ms`、warnings `0`、errors `0`；iPadOS `iPad Air 11-inch (M4)` status `SUCCEEDED`、duration `17754 ms`、warnings `0`、errors `0`。
  - 第 4 組完整回歸：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`；未帶 `-only-testing`，duration `79207 ms`，XcodeBuildMCP 回報 discovered `718`、passed `714`、failed `2`、skipped `0`，status `FAILED` 僅因既有 snapshot mismatch：`SnapshotTests/orderEditViewBaseline()`、`SnapshotTests/quoteViewBaseline()`。
  - 完整回歸原始 xcresult 核對：`xcrun xcresulttool get test-results summary --path /Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-18T18-49-16-972Z_pid21982_01442fb3.xcresult` 回報 `totalTestCount 716`、`passedTests 714`、`failedTests 2`、`skippedTests 0`。XcodeBuildMCP 未提供原始 xcresult 計數，故以 `xcresulttool` 核對口徑。
  - 與 task 0.1 baseline 比較：原始 XCTest 執行數 `707 → 716` (`+9`)，passed `704 → 714` (`+10`)，failed `3 → 2` (`-1`)，skipped 維持 `0`；失敗仍只有 baseline 的上述兩條，沒有新增失敗。

## 5. Core 檔案的規範對齊

- [x] 5.1 [after: 1.5, 3.1, 3.2] 讓 `Core/Dependencies/` 的 15 個檔案 (既有 14 檔加上新增的 `PhotoImportResult.swift`) 符合 ios-dev-kit，包含 findings.md 在既有檔案記錄的判讀項目 (doc 措辭、為測試放寬的存取層級、Bool 命名) 與機械項目；新增檔案不在 findings.md 內，直接以 ios-dev-kit 為準。依「機械規則只修本步動到的檔案」不擴散到其他目錄。驗證：對這 15 個檔案執行 design 列的三個機械檢查命令皆無輸出；需要判讀的項目逐檔對照 findings.md 與 ios-dev-kit 的 `references/formatting.md`、`references/coding-style.md` 核對，處理過的項目代碼逐檔記在本檔；專案可編譯。
  - 機械檢查（15 檔）：design.md 的 Python3 Unicode 行寬命令、尾隨空白/Tab 的 `awk` 命令、檔頭第 5 行日期格式的 `awk` 命令皆無輸出。
  - 判讀項目逐檔紀錄：`BiometricAuthClient.swift` — MK1×4、FM9×4、H1×1；`CalendarReminderClient.swift` — MK1×4、FM1×4、H1×1、D9×1、FM0×1；`CampaignReminderRepository.swift` — MK1×3、H1×1、FM1×1；`CampaignRepository.swift` — MK1×2、H1×1；`CategoryRepository.swift` — MK1×2、H1×1。
  - 判讀項目逐檔紀錄：`CurrencyMetadataRepository.swift` — FM1×6、MK1×3、D9×2、H1×1、MK4×1、MK5×1；`NameLookupOperations.swift` — H1×1；`OpenSettingsClient.swift` — MK1×2、H1×1；`OrderRepository.swift` — FM1×7、MK1×3、H1×1、MK4×1、MK5×1；`OrderSourceRepository.swift` — MK1×2、H1×1、FM1×1。
  - 判讀項目逐檔紀錄：`PaymentMethodRepository.swift` — FM1×4、MK1×2、H1×1；`PhotoClient.swift` — MK1×2、H1×1、IM3×1、FM0×1；`ReconciliationStatusRepository.swift` — FM1×4、MK1×2、H1×1；`TelemetryClient.swift` — MK1×2、H1×1；`PhotoImportResult.swift` — 新檔不在 findings.md，依 ios-dev-kit 逐項核對。
  - 實際驗證：本 task 的完整回歸見 5.3 末尾；本次回歸成功編譯專案，並回報原始 XCTest `totalTestCount 716`、`passed 714`、`failed 2`、`skipped 0`。
- [x] 5.2 [after: 2.3] 讓 `Core/Domain/` 的 16 個手寫檔 (Campaign、Campaign+Samples、CampaignStatus、CurrencyCode、CustomerTier、FxRates、FxRateSnapshot、LedgerOrder、LedgerOrder+Samples、LedgerOrderItem、OrderMerge、OrderStatus、OrderSummary、PaymentMethodFlags、PaymentMethodInfo、PaymentReceiptStatus) 與 `Core/Diagnostics/` 的 2 個檔 (AppLogger、CrashDiagnosticsClient) 符合 ios-dev-kit，包含 findings.md 記錄的判讀項目 (多餘參數標籤、tuple 元素數、`- Note:` 用法、縮寫大小寫、`Locale` 走 `@Dependency`)；`Core/Domain/Generated/` 由 generator 產生，不在此 task 手改。驗證：對這 18 個檔案執行三個機械檢查命令皆無輸出、判讀項目逐檔記錄，`OrderMergeTests` 與 `OrderCalculationTests` 全綠。
  - 機械檢查（18 檔）：design.md 的 Python3 Unicode 行寬命令、尾隨空白/Tab 的 `awk` 命令、檔頭第 5 行日期格式的 `awk` 命令皆無輸出。
  - 判讀項目逐檔紀錄：`Campaign+Samples.swift` — H1×1、D1×1；`Campaign.swift` — H1×1、MK1×1；`CampaignStatus.swift` — H1×1、MK1×1；`CurrencyCode.swift` — MK1×2、H1×1；`CustomerTier.swift` — H1×1、MK1×1；`FxRateSnapshot.swift` — H1×1、MK1×1。
  - 判讀項目逐檔紀錄：`FxRates.swift` — H1×1、MK1×1；`LedgerOrder+Samples.swift` — D1×2、H1×1、FM4×1、FM1×1、FM10×1；`LedgerOrder.swift` — H1×1、MK1×1、CL2×1（`contributesToCategoryBreakdown` 依 design.md accepted exception 保留）；`LedgerOrderItem.swift` — D1×3、MK1×2、H1×1；`OrderMerge.swift` — H1×1、MK1×1、D6×1；`OrderStatus.swift` — MK1×2、H1×1。
  - 判讀項目逐檔紀錄：`OrderSummary.swift` — H1×1、MK1×1、FM1×1、D6×1；`PaymentMethodFlags.swift` — MK1×2、H1×1；`PaymentMethodInfo.swift` — H1×1；`PaymentReceiptStatus.swift` — H1×1、MK1×1；`AppLogger.swift` — MK5×3、H1×1、MK4×1、MK1×1；`CrashDiagnosticsClient.swift` — MK1×2、H1×1。
  - 實際驗證：`OrderMergeTests` 10 passed、0 failed、0 skipped；`OrderCalculationTests` 15 passed、0 failed、0 skipped。
- [x] 5.3 [after: 1.4] 讓 `Core/Networking/` 的 8 個檔案 (APIError、AppConfiguration、ExchangeRateClient、ExchangeRateLatestResponse、ExchangeRateCodesResponse、HTTPClient、HTTPMethod、URLRequestBuilder) 符合 ios-dev-kit，包含 findings.md 記錄的判讀項目 (`CodingKeys` 放 Nested Types extension、錯誤分類、為測試放寬的存取層級、doc 摘要單行)。驗證：對這 8 個檔案執行三個機械檢查命令皆無輸出、判讀項目逐檔記錄，`HTTPClientTests`、`ExchangeRateClientTests`、`APIErrorMappingTests`、`AppConfigurationTests` 全綠。
  - 機械檢查（8 檔）：design.md 的 Python3 Unicode 行寬命令、尾隨空白/Tab 的 `awk` 命令、檔頭第 5 行日期格式的 `awk` 命令皆無輸出。
  - 判讀項目逐檔紀錄：`APIError.swift` — H1×1、MK1×1（`apiError` 等純分類 case 依 design.md 維持既有分類形狀）；`AppConfiguration.swift` — MK1×3、H1×1、D6×1；`ExchangeRateClient.swift` — TC1×5、MK1×2、D6×2、FM1×2、H1×1。
  - 判讀項目逐檔紀錄：`ExchangeRateLatestResponse.swift` — D1、MK1、H1；`ExchangeRateCodesResponse.swift` — D1、MK1、H1；兩檔合計重新核對原 `ExchangeRateDTO.swift` 的 D1×8、MK1×6、H1×1；`HTTPClient.swift` — MK1×3、FM1×2、H1×1、D1×1；`HTTPMethod.swift` — H1×1、MK1×1；`URLRequestBuilder.swift` — H1×1、MK1×1。
  - Review 修正：原先誤列為已處理的 `FN1×1` 已透過拆檔實際處理；`ExchangeRateLatestResponse.swift` 與 `ExchangeRateCodesResponse.swift` 各自只保留一個主要型別及自己的 `Nested Types` extension。原 `ExchangeRateDTO.swift` 已移除。
  - Review 修正：原 `ExchangeRateDTO.swift` 第 30 行與第 94 行對應的兩處「最後成員與收尾大括號間多餘空行」已在拆檔時移除。
  - 實際驗證：`HTTPClientTests` 3 passed、0 failed、0 skipped；`ExchangeRateClientTests` 9 passed、0 failed、0 skipped；`APIErrorMappingTests` 4 passed、0 failed、0 skipped；`AppConfigurationTests` 7 passed、0 failed、0 skipped。
  - 實際驗證：以 `&&` 序列化執行 iOS 與 iPadOS build；iOS `iPhone 17` status `SUCCEEDED`、duration `56722 ms`、warnings `0`、errors `0`；iPadOS `iPad Air 11-inch (M4)` status `SUCCEEDED`、duration `16643 ms`、warnings `0`、errors `0`。
  - 第 5 組修正後完整回歸：命令為 `xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`，未帶 `-only-testing`；xcresult `startTime` 為 2026-09-19 04:38:38 +08:00，duration `83042 ms`，status `FAILED`（僅因 snapshot mismatch）；XcodeBuildMCP 回報 discovered `718`、passed `714`、failed `2`、skipped `0`。
  - 完整回歸的 xcresult：`/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-18T20-37-36-408Z_pid12994_1643c949.xcresult`；`xcrun xcresulttool get test-results summary` 回報原始 XCTest `totalTestCount 716`、`passedTests 714`、`failedTests 2`、`skippedTests 0`。XcodeBuildMCP 未提供原始 xcresult 計數，故以 xcresulttool 核對口徑。
  - 與 task 0.1 baseline `totalTestCount 707`、`passed 704`、`failed 3`、`skipped 0` 比較：測試數 `+9`、通過數 `+10`、失敗數 `-1`、skipped `±0`；失敗仍只有 baseline 既有的 `SnapshotTests/orderEditViewBaseline()` 與 `SnapshotTests/quoteViewBaseline()`，沒有新增失敗。
- [x] 5.4 [after: 2.1] 讓 `Core/Persistence/` 除 `BuyLedgerSchema.swift` 以外的 20 個檔案 (既有 19 檔加上新增的 `RecordDecodingError.swift`) 符合 ios-dev-kit，包含 findings.md 記錄的判讀項目 (成員從 private 起手、`fatalError` 不用於執行期狀況、泛型參數具名、角色 protocol 命名)。驗證：對這 20 個檔案執行三個機械檢查命令皆無輸出、判讀項目逐檔記錄，持久化相關測試全綠。
  - 機械檢查（20 檔）：依 design.md 的 Python3 Unicode 行寬、尾隨空白/Tab、檔頭第 5 行日期格式三個命令執行，三個命令皆無輸出。
  - 判讀項目逐檔紀錄：`CampaignPersistence.swift` — H1×1；已改寫 actor 職責 doc。`CampaignRecord.swift` — MK1×2、H1×1；`??` 於 task 2.1 已移除，本次複核仍無 `??`。`CampaignReminderPersistence.swift` — H1×1；已改寫 actor 職責 doc。`CampaignReminderRecord.swift` — H1×1、MK1×1。`CategoryRecord.swift` — H1×1、MK1×1；尾端遵循改為 `NameLookupRecordProtocol`。
  - 判讀項目逐檔紀錄：`CurrencyMetadataPersistence.swift` — H1×1、CL1×1；已修正 `- Note:` 順序與排序 closure 參數。`CurrencyMetadataRecord.swift` — H1×1、MK1×1。`NameLookupPersistence.swift` — H1×1、CL1×1；已修正排序 closure 與說明文字。`NameLookupRecordProtocol.swift`（原 `NameLookupRecord.swift`）— H1×1、MK1×1、MK4×1；角色 protocol 改為 `NameLookupRecordProtocol`，並維持所有 `@Model` 遵循在各記錄檔尾端 extension，未改任何 model 主體。
  - 判讀項目逐檔紀錄：`OrderPersistence.swift` — H1×1、FM4×1、FM1×1；查詢 helper 收回 private extension，測試改驗證公開行為，未改 `try? rollbackPendingOrderRecords`。`OrderRecord.swift` — MK1×2、H1×1；`??` 於 task 2.1 已移除，本次複核仍無 `??`。`OrderSourceRecord.swift` — H1×1、MK1×1；尾端遵循改為 `NameLookupRecordProtocol`。`PaymentMethodPersistence.swift` — CL2×4、CL1×2、D6×2、H1×1、FM1×1；移除不可達 `fatalError`、泛型參數改 `Value`、補具名 closure 與白話 doc。`PaymentMethodRecord.swift` — H1×1、MK1×1。
  - 判讀項目逐檔紀錄：`PersistenceContainer.swift` — MK5×2、D3×2、H1×1、MK1×1、D9×1、FM1×1；`inMemoryOnly` 改為 `isInMemoryOnly`，並修正 MARK、doc 與行寬。`PersistenceError.swift` — D9×11、MK1×5、MK4×5、MK5×5、H1×1；移除 `Cases` MARK，將 `LocalizedError` 實作移至獨立 extension，補齊 associated value doc。`PersistenceStoreQuarantine.swift` — H1×1、D6×1；改寫資料庫/附屬檔案的白話 doc。`PersistenceStoreQuarantineClient.swift` — MK1×2、H1×1。`ReconciliationStatusRecord.swift` — H1×1、MK1×1；尾端遵循改為 `NameLookupRecordProtocol`。`RecordDecodingError.swift` — 新檔不在 findings.md，依 ios-dev-kit 逐項核對。
  - 必要介面適配：同步更新 allowlist 內的 `Core/Dependencies/NameLookupOperations.swift` 與 `NameLookupPersistenceTests.swift` 的 protocol 約束；`OrderPersistenceTests.swift` 不再存取 private helper，改驗證 `fetchAll` 與 `upsertAll` 的公開行為。
  - 實際驗證命令：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --extra-args "-only-testing:BuyLedgerTests/CampaignPersistenceTests" --extra-args "-only-testing:BuyLedgerTests/NameLookupPersistenceTests" --extra-args "-only-testing:BuyLedgerTests/OrderPersistenceTests" --extra-args "-only-testing:BuyLedgerTests/PaymentMethodPersistenceTests" --extra-args "-only-testing:BuyLedgerTests/PersistenceErrorTests" --extra-args "-only-testing:BuyLedgerTests/PersistenceFailureFeatureTests" --extra-args "-only-testing:BuyLedgerTests/PersistenceRecoveryTests" --extra-args "-only-testing:BuyLedgerTests/RecordDecodingTests" --extra-args "-only-testing:BuyLedgerTests/SchemaMigrationTests" --output json --progress false`；status `SUCCEEDED`、duration `57060 ms`，共 `89 passed`、`0 failed`、`0 skipped`。
- [x] 5.5 [after: 5.4] 讓 `Core/Persistence/BuyLedgerSchema.swift` 符合 ios-dev-kit 的 MARK 分區、doc 與排版規範，schema 版本與 migration stage 的內容不變。驗證：對該檔執行三個機械檢查命令皆無輸出、判讀項目記錄在本檔，`SchemaMigrationTests` 全綠且未新增 schema 版本。
  - 機械檢查（`BuyLedgerSchema.swift`）：依 design.md 的 Python3 Unicode 行寬、尾隨空白/Tab、檔頭第 5 行日期格式三個命令執行，三個命令皆無輸出。
  - 判讀項目紀錄：已處理 D1×98、FM10×82、MK1×12、MK5×5、MK4×2、H1×1、FM4×1；shadow 的 91 個 stored properties 與 7 個 initializer 均補 doc，屬性間補單一空行，MARK 改為規範名稱。
  - 保護項目紀錄：`FN1×1`（多個保留 schema 型別與 migration plan 共置）與 `C3×1`（migration 使用的 `nonisolated(unsafe)` 暫存）未列為已處理；依本 task 只允許 MARK/doc/排版且 schema 版本與 migration stage 內容一字不動的限制保留原樣。未新增 schema 版本；版本仍為 V15、V16、V17，migration stage 仍為一個 custom 與一個 lightweight。
  - schema 內容確認：修改前後排除註解與空行的 code-only SHA-256 皆為 `65b525169f264e6b477a3cf102101c448d35f8fe09c2e86323042640385f1b10`，共 `348` 行。
  - 實際驗證：`SchemaMigrationTests` status `SUCCEEDED`、`7 passed`、`0 failed`、`0 skipped`；已包含在 5.4 的 89 項持久化相關測試中。
  - 最後完整回歸：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`，未帶 `-only-testing`，duration `32939 ms`；XcodeBuildMCP 回報 discovered `718`、passed `714`、failed `2`、skipped `0`，失敗只有 baseline 的 `SnapshotTests/orderEditViewBaseline()`、`SnapshotTests/quoteViewBaseline()`。
  - 完整回歸原始 xcresult 核對：`xcrun xcresulttool get test-results summary --path /Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-18T21-07-02-683Z_pid36927_0178e34d.xcresult` 回報 `totalTestCount 716`、`passedTests 714`、`failedTests 2`、`skippedTests 0`。XcodeBuildMCP 未提供原始 xcresult 計數，故以 `xcresulttool` 核對口徑。
  - 與 task 0.1 baseline `totalTestCount 707`、`passed 704`、`failed 3`、`skipped 0` 比較：測試數 `+9`、通過數 `+10`、失敗數 `-1`、skipped `±0`；沒有新增失敗。

## 6. 測試與驗收

- [x] 6.1 [after: 5.1, 5.2, 5.3, 5.4, 5.5] 讓 Core 對應的單元測試符合 ios-dev-kit 的 Swift Testing 寫法、測試命名、fixture 命名與 doc 規範，且斷言改以 `if case` 比對錯誤 case 而非整值比較。涵蓋的測試檔清單 (依前面 task 實際動到的檔案決定) 寫進本檔。驗證：對清單上的測試檔執行三個機械檢查命令皆無輸出，且測試數與通過數不低於 0.1 記下的 baseline。
  - 涵蓋檔案清單（前面 task 實際動到的 24 個測試／fixture 支援檔）：
    - `apps/ios/BuyLedgerTests/AISummaryFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/APIErrorMappingTests.swift`
    - `apps/ios/BuyLedgerTests/CampaignFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/CampaignReminderFailureTests.swift`
    - `apps/ios/BuyLedgerTests/CurrencyMetadataCacheTests.swift`
    - `apps/ios/BuyLedgerTests/ExchangeRateClientTests.swift`
    - `apps/ios/BuyLedgerTests/FxRatesTests.swift`
    - `apps/ios/BuyLedgerTests/HTTPClientTests.swift`
    - `apps/ios/BuyLedgerTests/LookupManagementFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/NameLookupPersistenceTests.swift`
    - `apps/ios/BuyLedgerTests/OrderCalculationTests.swift`
    - `apps/ios/BuyLedgerTests/OrderEditFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/OrderMergeFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/OrderPersistenceTests.swift`
    - `apps/ios/BuyLedgerTests/OrdersFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/OrdersLoadStateTests.swift`
    - `apps/ios/BuyLedgerTests/PaymentMethodPersistenceTests.swift`
    - `apps/ios/BuyLedgerTests/PersistenceErrorTests.swift`
    - `apps/ios/BuyLedgerTests/PersistenceFailureFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/PersistenceRecoveryTests.swift`
    - `apps/ios/BuyLedgerTests/QuoteFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/RecordDecodingTests.swift`
    - `apps/ios/BuyLedgerTests/RootFeatureTests.swift`
    - `apps/ios/BuyLedgerTests/TestDependencies.swift`（fixture／dependency 支援檔，本身沒有 `@Test`）
  - 實際核對：共 `368` 個 `@Test`，`368` 個 Given、`368` 個 When、`368` 個 Then；`368` 個測試皆有 `///` doc，測試方法名稱含底線 `0` 處。錯誤 case 的 payload／分類比較保留為 `switch` 或 `if case`，未以整個錯誤值比較，也未把具體斷言改成恆真或只驗非空。
  - 機械檢查（上述 24 檔）：依 design.md 的 Python3 Unicode 行寬命令、尾隨空白/Tab 的 `awk` 命令、檔頭第 5 行日期格式的 `awk` 命令執行，三個命令皆無輸出。
  - 守門力核對：既有具體的欄位、狀態、數量、錯誤 payload 與 domain/code/localizedDescription 斷言均保留；`apps/ios/BuyLedgerTests/OrderCalculationTests.swift:529` 的既有弱守門 `#expect(corrected.summary.revenue >= 0)` 未改動，標記為本 change 範圍外，不列為本 task 已處理項目。

- [x] 6.2 [after: 4.2, 6.1] 完成本 change 的驗收：在最後一次改檔之後執行完整單元測試回歸並逐筆核對結果，執行 `bun run check` 確認 codegen 無漂移，並逐檔確認 findings.md 的判讀違規與機械必擋都已處理，只剩「本 change 接受的規範例外」明列的兩條 (`$newUUID` 預設值、`contributesToCategoryBreakdown` 命名)，以及該節列出的 18 筆既有登記例外 (17 筆 TC4 宣告 testValue 加 1 筆 PersistenceContainer 的禁 .shared)。驗證：回歸結果、`check` 退出碼與逐檔處理紀錄都寫在本檔，回歸的執行時間晚於最後一次 Swift 檔寫入時間，且與 0.1 的 baseline 完成數字比較。
  - 最後一次 Swift 檔寫入：`2026-09-19 05:46:38 +0800`（24 檔中最新為 `apps/ios/BuyLedgerTests/RootFeatureTests.swift`）。其後未再修改 Swift 檔。
  - 最終完整回歸命令：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`；未帶 `-only-testing`。2026-09-19 05:50:05 +0800 開始，duration `30782 ms`，XcodeBuildMCP status `FAILED` 僅因 snapshot mismatch，回報 discovered `718`、passed `714`、failed `2`、skipped `0`；失敗逐筆為 `SnapshotTests/orderEditViewBaseline()`、`SnapshotTests/quoteViewBaseline()`。
  - 原始 xcresult：`/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-18T21-49-52-039Z_pid72217_184acc05.xcresult`；因 XcodeBuildMCP 未提供原始 xcresult 計數，使用唯讀 `xcrun xcresulttool get test-results summary` 核對為 `totalTestCount 716`、`passedTests 714`、`failedTests 2`、`skippedTests 0`。回歸開始時間晚於最後一次 Swift 檔寫入時間。
  - 與 task 0.1 baseline `totalTestCount 707`、`passed 704`、`failed 3`、`skipped 0` 比較：測試數 `+9`、通過數 `+10`、失敗數 `-1`、skipped `±0`；最終回歸沒有新增失敗。此前同一份未變更的 Swift 程式在第一次完整回歸曾暫時出現 `orderEditViewMergeContextBaseline()` mismatch，立即重跑後消失，故不列入最終失敗清單或本 change 的新增失敗。
  - generator 驗證：在 `shared/data-model/generator` 執行 `bun run check`，exit code `0`；輸出 `check 通過：磁碟產出與 schema 同步`。
  - findings.md 逐檔驗收：5.1–5.5 已在本檔逐檔記錄 findings 的判讀項目代碼與處理結果，並重跑各 task 的三項機械檢查；6.1 對上述 24 個測試／fixture 支援檔重跑三項檢查皆無輸出。除下列明列項目外，沒有未處理的 findings 項目：
    - design.md accepted exception：`$newUUID` 生成的 `UUID = UUID()` 預設值、`LedgerOrder.contributesToCategoryBreakdown` 命名。
    - 5.5 保護項目：`BuyLedgerSchema.swift` 的 `FN1×1` 與 `C3×1`。
    - 既有登記例外 18 筆：17 筆 TC4 宣告 `testValue`，以及 `PersistenceContainer.swift` 禁用 `.shared` 1 筆。
  - 確認結論：上述 2 + 2 + 18 項之外沒有默默略過或新增的未處理項目；本 change 的 6.1 與 6.2 驗收完成。
  - Review 後修正（8 項）已完成：HTTPClient 與 ExchangeRateClient 的 App 自有診斷錯誤改用 `com.leoho.BuyLedger.networking` 與區分用 code，HTTPClient 移除 `transportError(underlying:)`；同步 `apps/ios/CLAUDE.md` 的 `shouldSeedSampleOrders` 名稱；修正 PersistenceErrorTests 的 Given/When/Then 排版；補上 containerCreationFailed、directoryResolutionFailed、storage(.saveFailed) rollback 與 photo failure count reset 測試；RecordDecodingTests 覆蓋空 rawValue，ExchangeRateClientTests 比對缺欄位解碼錯誤 domain；LedgerOrder samples 改用不會失敗的 Decimal 建構；移除 CurrencyCode.localizedName() 與 Core 領域檔的 TCA import。
  - Review 修正後針對性測試：以 XcodeBuildMCP 執行 `HTTPClientTests`、`ExchangeRateClientTests`、`PersistenceErrorTests`、`RecordDecodingTests`、`PersistenceRecoveryTests`、`PersistenceFailureFeatureTests`、`OrderPersistenceTests`、`PaymentMethodPersistenceTests`、`OrderEditFeatureTests`；status `SUCCEEDED`，duration `66144 ms`，`125 passed`、`0 failed`、`0 skipped`。該次測試後再移除兩處新增的 `as` 永遠為真的編譯警告，修正後版本已包含在後續完整回歸與 iPadOS build。
  - Review 修正後機械檢查：對本輪實際修改的 `13` 個 Swift 檔執行 design.md 的 Python3 Unicode 行寬、尾隨空白/Tab、檔頭日期三個命令，皆無輸出。
  - Review 修正後最後一次 Swift 檔寫入為 `2026-09-19 13:06:46 +0800`（`HTTPClientTests.swift`、`ExchangeRateClientTests.swift`）；其後未再修改 Swift 檔。
  - Review 修正後完整回歸：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`；未帶 `-only-testing`，於 `2026-09-19 13:07:29 +0800` 開始，duration `50077 ms`；XcodeBuildMCP status `FAILED`，discovered `721`、passed `718`、failed `2`、skipped `0`，失敗仍只有 `SnapshotTests/orderEditViewBaseline()` 與 `SnapshotTests/quoteViewBaseline()`。
  - 上述回歸的 xcresult：`/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-19T05-07-29-598Z_pid89043_3b9f4a86.xcresult`；因 XcodeBuildMCP 未提供原始計數，使用唯讀 `xcrun xcresulttool get test-results summary` 核對為 `totalTestCount 720`、`passedTests 718`、`failedTests 2`、`skippedTests 0`。與 task 0.1 baseline `707 / 704 / 3 / 0` 比較：測試數 `+13`、通過數 `+14`、失敗數 `-1`、skipped `±0`，沒有新增失敗。
  - Review 修正後 iPadOS build：`xcodebuildmcp --log-level error simulator build --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-id 6B65ED1C-3C2E-42BA-B1E7-08F606F175C5 --output json`；於 `2026-09-19 13:08:54 +0800` 執行，iPad Air 11-inch (M4) simulator status `SUCCEEDED`，duration `58454 ms`，warnings `0`、errors `0`。
  - 最終 review 後的 Given/When/Then 修正：針對 `AISummaryFeatureTests.swift`、`APIErrorMappingTests.swift`、`CampaignFeatureTests.swift`、`FxRatesTests.swift`、`LookupManagementFeatureTests.swift`、`NameLookupPersistenceTests.swift`、`OrderCalculationTests.swift`、`OrderEditFeatureTests.swift`、`OrderMergeFeatureTests.swift`、`OrderPersistenceTests.swift`、`OrdersFeatureTests.swift`、`OrdersLoadStateTests.swift`、`PaymentMethodPersistenceTests.swift`、`PersistenceRecoveryTests.swift`、`QuoteFeatureTests.swift`、`RootFeatureTests.swift` 共 16 檔，修正原先 `// When` 空標記 74 處與 `// Then` 空標記 62 處；保留空的 `// Given`。TCA 測試的 `// Then` 已移入 trailing closure 或實際 `#expect` 前；原本只有 no-op action 的案例改為具體狀態斷言，未加入恆真斷言，也沒有發現真正只有動作而無守門斷言的測試。
  - 標記驗證：以 Python 掃描所有 `apps/ios/BuyLedgerTests/**/*.swift`，要求每個 `// When`／`// Then` 後的第一個非空行不是另一個註解或 `}`；輸出 `empty-marker-count 0`。
  - 最終 6.1 清單 24 檔的三項機械檢查：依 design.md 的 Python3 Unicode 行寬、尾隨空白/Tab、檔頭日期格式命令執行，三個命令皆無輸出。最後一次 Swift 檔寫入為 `2026-09-19 13:38:33 +0800`（`apps/ios/BuyLedgerTests/OrderEditFeatureTests.swift`）。
  - 最終完整回歸：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`；未帶 `-only-testing`，於 `2026-09-19 13:40:24 +0800` 開始，duration `34640 ms`；XcodeBuildMCP status `FAILED`，discovered `721`、passed `717`、failed `3`、skipped `0`。失敗為 `SnapshotTests/orderEditViewBaseline()`、`SnapshotTests/orderEditViewLongIdentifierBaseline()`、`SnapshotTests/quoteViewBaseline()`。
  - 上述回歸的 xcresult：`/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-19T05-40-24-557Z_pid17249_db16117a.xcresult`；因 XcodeBuildMCP 未提供原始 XCTest total，使用唯讀 `xcrun xcresulttool get test-results summary` 核對為 `totalTestCount 720`、`passedTests 717`、`failedTests 3`、`skippedTests 0`。回歸開始時間晚於最後一次 Swift 檔寫入時間。
  - 與 task 0.1 baseline `totalTestCount 707`、`passed 704`、`failed 3`、`skipped 0` 比較：測試數 `+13`、通過數 `+13`、failed 總數 `±0`、skipped `±0`，但失敗清單組成已改變：baseline 的 `SnapshotTests/ordersCompactViewMultiSelectBaseline()` 已轉綠，改由本輪新出現的 `SnapshotTests/orderEditViewLongIdentifierBaseline()` 取代；另兩條 `orderEditViewBaseline()` 與 `quoteViewBaseline()` 持續失敗。本輪沒有修改 snapshot；`orderEditViewLongIdentifierBaseline` 在單獨選取的 SnapshotTests 回歸中通過（SnapshotTests 共 `12 passed`、`2 failed`、`0 skipped`，失敗只有 `orderEditViewBaseline` 與 `quoteViewBaseline`），但完整套件仍重現該條 mismatch，故不能將它列為已解決或 baseline 既有失敗。
  - Review 後追蹤 `orderEditViewLongIdentifierBaseline()` 干擾：`BuyLedger.xctestplan` 的 `testExecutionOrdering` 為 `alphabetical`；依序以 SnapshotTests 加嫌疑 suite 做二分。前半 9 個 suite 加 SnapshotTests 為 `158 passed / 2 failed`（只有既有 `orderEditViewBaseline`、`quoteViewBaseline`）；後半 8 個 suite 為 `168 passed / 3 failed`（多出 long-ID）。再將後半拆成前 4 個為 `135 / 2`、後 4 個為 `46 / 2`，均沒有 long-ID；逐步加入 `PersistenceErrorTests`、`PersistenceRecoveryTests`、`QuoteFeatureTests` 的 prefix 結果分別為 `283 / 2`、`292 / 3`、`311 / 3`，出現的是另一條間歇性的 `orderEditViewMergeContextBaseline`，不是穩定 long-ID 來源。
  - 方法層選取均使用帶括號的 `-only-testing:BuyLedgerTests/SnapshotTests/orderEditViewLongIdentifierBaseline()`；全部 Snapshot 之前的 test suites 加 exact long-ID 為 `707 passed / 0 failed / 0 skipped`，同一批 suites 加完整 SnapshotTests 為 `718 passed / 2 failed / 0 skipped`，失敗只有上述兩條既有 baseline。這排除了某一個 alphabetical 前置 suite 能穩定污染 long-ID 的結論；`PersistenceContainer.shared` 也沒有在這條路徑形成可重現的因果。
  - 程式碼檢查仍發現真正的共享狀態外溢：`RootFeatureTests.swift` 有 11 個測試直接改寫 `@Shared(.lookupCatalog)`，`OrdersFeatureTests.swift` 的 `filterSheetSearchTextFiltersCategoriesAndPaymentMethods()` 也直接改寫；`LookupManagementFeatureTests` 已有 `withIsolatedCatalog`，不在修正範圍。修正方式是新增 `RootFeatureTests.makeIsolatedRootState`，以 `defaultInMemoryStorage = InMemoryStorage()` 建立隔離 state；11 個 Root 測試改用它，新增 OrderEdit 子 state 的測試則在同一隔離 scope 建立 Root 與子 state。Orders 的該測試包在獨立 `InMemoryStorage` scope。沒有修改 `SnapshotTests.swift`，也沒有更新任何 snapshot 基準圖。
  - 修正後 targeted 驗證：`RootFeatureTests`、`OrdersFeatureTests` 與 exact `orderEditViewLongIdentifierBaseline()` 合併執行，`105 passed / 0 failed / 0 skipped`；兩個修改檔依 design.md 的 Python3 Unicode 行寬、尾隨空白/Tab、檔頭日期三項命令重跑，三項皆無輸出。
  - 修正後第一次完整回歸曾短暫出現 `717 passed / 3 failed`，第三條為 `orderEditViewMergeContextBaseline()`；未修改程式或 snapshot，立即重跑後該條消失。最後一次完整回歸：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-name "iPhone 17" --use-latest-os true --output json --progress false`，未帶 `-only-testing`，XcodeBuildMCP 於 `2026-09-19 14:51:48 +0800` 開始，duration `36116 ms`，回報 discovered `721`、`718 passed / 2 failed / 0 skipped`；唯讀 `xcrun xcresulttool get test-results summary` 的 `totalTestCount` 為 `720`，失敗逐筆為 `SnapshotTests/orderEditViewBaseline()`、`SnapshotTests/quoteViewBaseline()`，`orderEditViewLongIdentifierBaseline()` 已通過。xcresult：`/Users/leoho/Library/Developer/XcodeBuildMCP/workspaces/BuyLedger-61c22e57ebf6/result-bundles/test_sim_2026-09-19T06-51-48-819Z_pid75711_3fc1c278.xcresult`。
  - 與 task 0.1 baseline `707 / 704 / 3 / 0` 比較，最後一次回歸為 `720 / 718 / 2 / 0`：測試數 `+13`、通過數 `+14`、失敗數 `-1`、skipped `±0`；baseline 的 `ordersCompactViewMultiSelectBaseline()` 已轉綠，long-ID 也已轉綠，最終只剩 baseline 的 `orderEditViewBaseline()` 與 `quoteViewBaseline()`。中間一次 `orderEditViewMergeContextBaseline()` 的瞬時 mismatch 已在立即重跑消失，沒有被列為最終失敗或默默忽略。
  - 最終裁決後重錄前置：以 `xcodebuildmcp simulator-management set-appearance --simulator-id DDAA3311-B464-4DD3-96B8-360B26AF1929 --mode light --output json` 鎖定 iPhone 17 simulator 為淺色，status `SUCCEEDED`。
  - 僅重錄 `SnapshotTests/orderEditViewBaseline()` 與 `SnapshotTests/quoteViewBaseline()`：兩次均使用帶括號的 method selector，並以 XcodeBuildMCP test-runner environment 傳入 `SNAPSHOT_TESTING_RECORD=all`。兩次各 discovered `1`、各實際執行 `1` 條；SnapshotTesting 回報 `Record mode is on. Automatically recorded snapshot` 的預期失敗，並寫入指定 PNG。未修改 `SnapshotTests.swift`。
  - 重錄理由：`quoteViewBaseline.1.png` 的舊參考圖錯誤，成本拆解最後兩項只有 `$23`、`$23`，缺少「金流手續費」與「平台手續費」標籤；新截圖包含正確的兩個標籤，因此重錄是修正錯誤基準圖。`orderEditViewBaseline.1.png` 的差異是 iOS 27 SDK 在已設定為 `zh_TW` 的 simulator 上，DatePicker 由 `2026年4月26日` 改為緊湊格式 `2026/4/26`；這是 SDK 的地區日期呈現變化，不是產品缺陷，畫面下方 App 自己格式化的 `訂購日期：2026/04/26 08:00:00` 未變。
  - PNG 範圍核對：重錄前後對 `apps/ios/BuyLedgerTests/__Snapshots__/SnapshotTests/*.png` 全部 14 張執行 `shasum -a 256`；只有兩個 allowlist 目標變更：`orderEditViewBaseline.1.png`：`b4f0c14166c6ab3b3579a3fbc34d49d182e00e3f832df29c0af46703106ab4bd` → `0b373a269ea32c390bae86270f2db2c69609c4c081baf429845c5fea36490019`；`quoteViewBaseline.1.png`：`4c19914d33262e203794bac47bc10977c2e03869db019a1b39b01a9788d201e3` → `037a013c4fd7ec2e8de8d0681facba4fe2ac353e36fd84eccefb642aef8d95df`。其餘 12 張 hash 完全不變。
  - 重錄後完整回歸：`xcodebuildmcp --log-level error simulator test --project-path apps/ios/BuyLedger.xcodeproj --scheme BuyLedger --simulator-id DDAA3311-B464-4DD3-96B8-360B26AF1929 --output json --progress false`，未帶 `-only-testing`；XcodeBuildMCP status `SUCCEEDED`、discovered `721`、`720 passed`、`0 failed`、`0 skipped`，duration `37584 ms`。唯讀 `xcrun xcresulttool get test-results summary` 核對同一 xcresult 的 `totalTestCount 720`、`passedTests 720`、`failedTests 0`、`skippedTests 0`；兩條重錄的 baseline 均通過，其餘 baseline 未被重錄。
