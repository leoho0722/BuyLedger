## 1. 導航路徑唯一化

- [x] 1.1 依決策〈更多分頁改用值導向堆疊，深連結與清單共用同一條路徑〉，新增更多分頁的路徑列舉並以堆疊狀態承載，清單點擊與 AI 提示深連結皆寫入同一條路徑，深連結時於同一次狀態更新內先清空再推入，並移除原本的深連結呈現旗標。滿足 Each destination is reachable through a single path。行為：從清單進入設定後再觸發深連結，畫面上只有一份設定頁且返回一次回到分頁根層。驗證：以 TestStore 斷言深連結時路徑先清空再推入且結果只含單一設定項；實機重現原始情境確認返回路徑正確，並逐一測試所有進入設定頁的路徑無遺漏。
- [x] 1.2 依決策〈側邊欄併入單一選取型別〉，讓 RootSidebarLayout 以單一列舉表達分頁與智慧分組兩種項目並統一標記，確保兩者標記不重複；同時讓 RootFeature 的智慧分組動作不再覆寫既有的日期區間與類別篩選。滿足 Selection has a single source of truth。行為：任一時刻只有一列呈現選取態，選擇智慧分組不改變使用者既有篩選。驗證：以 TestStore 斷言智慧分組動作後日期區間與類別維持原值；實機逐一切換五個分頁與各智慧分組確認只高亮一列，並確認緊湊尺寸的分頁切換不受影響。
- [x] 1.3 依決策〈照片檢視改以推進呈現，取代第二層 modal〉，將 OrderEditView 的照片檢視加入既有的路徑列舉改以推進呈現，移除原本的全螢幕呈現。滿足 Only one modal layer is presented at a time。行為：開啟照片檢視時畫面上不存在兩層 modal，返回一次回到表單。驗證：實機開啟照片檢視確認為推進呈現且返回回到表單；並在有未儲存變更的情況下進出照片檢視，確認未儲存變更防護仍生效。
- [x] 1.4 依決策〈主檔管理的新增與編輯併入單一呈現狀態〉，將新增併入 LookupManagementDestination 列舉，改以單一呈現點依型別分派內容，移除獨立的新增呈現旗標。滿足 Only one modal layer is presented at a time。行為：主檔管理頁任一時刻只呈現一張 sheet，互斥由型別系統保證。驗證：以 TestStore 斷言呈現狀態為單一值不可同時表達兩者；實機連續觸發新增與編輯確認不發生 sheet 被靜默吞掉。

## 2. 回歸系統慣例

- [x] 2.1 [P] 依決策〈iPad 批次操作改放頂部，筆數改由標題承載〉，將 OrdersView 的批次操作由底部工具列改為頂部位置，選取筆數改由導覽標題承載並與緊湊尺寸共用同一來源。滿足 Controls are not placed where the window edge can hide them。行為：拖曳視窗使下緣超出螢幕時批次操作仍可觸及，選取筆數在兩種尺寸一致顯示於標題。驗證：實機於 iPad 進入多選並拖曳視窗確認操作可觸及；比對兩種尺寸的筆數顯示一致。
- [ ] 2.2 [P] 依決策〈設定頁交回系統返回鍵〉，移除 SettingsView 的自繪返回鈕與返回鍵隱藏設定，改以系統提供的編輯型工具列角色避免顯示過期的返回標題。滿足 System back navigation is preserved。行為：設定頁可由左緣右滑返回，且切換語言後返回鍵不顯示過期標題。驗證：實機於設定頁以左緣右滑返回；切換 App 語言後再進入設定頁確認返回鍵標題正確。
- [x] 2.3 [P] 依決策〈新增與重新命名改用 sheet 內表單而非 alert〉，將 LookupManagementView 的新增與重新命名改為 sheet 內的單欄表單，比照同目錄既有的付款方式編輯器提供取消與儲存。滿足 Alerts are not used to host forms。行為：新增與重新命名以表單呈現，重新命名預填目前名稱。驗證：實機於最大無障礙字級操作新增與重新命名，確認不需捲動即可完成；確認付款方式的開關類欄位不再遺失。
- [x] 2.4 [P] 讓 OrderFilterSheet 的搜尋提示描述與實際過濾範圍一致，或加入明確的範圍控制使兩者相符。滿足 Search scope is disclosed accurately。行為：使用者可從提示得知搜尋涵蓋哪些區塊。驗證：實機輸入付款方式關鍵字確認過濾結果與提示描述相符。

## 3. 破壞性動作安全網

- [x] 3.1 依決策〈主檔刪除加入確認並改為寫入成功後才更新狀態〉，為 LookupManagementFeature 加入刪除確認的警示狀態，文案點明會影響既有訂單，並將刪除改為資料庫寫入成功後才更新狀態與各處的記憶體副本。滿足 Destructive actions are confirmed consistently across the app 與 Deletion updates state only after persistence succeeds。行為：刪除前出現確認、取消不改變任何狀態、寫入失敗時項目仍在畫面上並顯示失敗訊息。驗證：以 TestStore 撰寫測試涵蓋確認流程、取消不變更狀態、以及寫入失敗時狀態不變三種情形；實機以注入失敗的 repository 確認失敗時項目仍可見。若刪除的視覺回饋出現可感知延遲，依 Risks 所述補上進行中指示而非退回樂觀更新。

## 4. 收尾與驗收

- [x] 4.1 將本次新增的刪除確認、失敗訊息與搜尋提示等字串補進 Localizable.xcstrings 的中英值，採文字插入方式而非全量重新序列化。行為：英文模式不露出中文 fallback。驗證：LocalizationCatalogTests 通過，並人工確認新增字串確實已收錄。
- [x] 4.2 重錄受呈現方式與工具列擺位變更影響的 snapshot baseline，重錄前逐張檢視差異確認變化符合預期。行為：baseline 與新實作一致。驗證：snapshot 測試全綠，差異檢視結論記錄於 commit 說明。
- [x] 4.3 依文件同步鐵則檢視本次 diff：將「每個目的地只有一條路徑」、「選取狀態單一來源」與「破壞性刪除先寫後改、失敗不留下不一致」寫入 apps/ios/CLAUDE.md 對應章節，並更新該檔中關於 sheet 內選擇器與 bottomBar 擺位的既有描述使其與現況一致。行為：文件無與現況矛盾的內容。驗證：對照根目錄 CLAUDE.md 的文件同步表逐列確認，結論為「有影響，已同步」或「確認無文件影響」。
